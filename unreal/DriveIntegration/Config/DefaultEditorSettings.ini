[/Script/UnrealEd.EditorPerformanceSettings]
; SimCore control heartbeats use the game tick during PIE. Unreal's default
; background throttling can reduce PIE to about 3 FPS, exceed the 250 ms
; command timeout, and repeatedly trigger SafeStop while testing locally.
bThrottleCPUWhenNotForeground=False
