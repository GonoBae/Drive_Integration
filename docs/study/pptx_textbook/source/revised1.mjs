import original from './volume1.mjs';

const P = {
  proto: 'protocol/vehicle.proto', main: 'cpp/host/src/main.cpp',
  host: 'cpp/host/src/simulation_host.hpp', session: 'cpp/host/src/simulation_host_session.cpp',
  physics: 'cpp/host/src/physics/vehicle_physics.cpp', ground: 'cpp/host/src/terrain/ground_query.hpp',
  decoder: 'cpp/host/src/protocol/vehicle_message_decoder.cpp', wire: 'cpp/host/src/protocol/vehicle_messages.cpp',
  lease: 'cpp/host/src/control/control_lease.hpp',
  client: 'unreal/DriveIntegration/Source/DriveIntegration/SimCoreClientComponent.cpp',
  parser: 'unreal/DriveIntegration/Source/DriveIntegration/SimCoreProtocol.cpp',
  test: 'cpp/host/tests/vehicle_protocol_test.cpp', cmake: 'cpp/host/CMakeLists.txt',
};
const ref = (path, symbol, start, end) => ({path, symbol, start, end});
const refs = (...numbers) => {
  const found = numbers.flatMap(number => original.slides.find(slide => slide.id === `V1-${String(number).padStart(2, '0')}`).sources);
  return [...new Map(found.map(source => [JSON.stringify(source), source])).values()];
};
const page = (title, body, sources, extra = {}) => ({
  type: 'lesson', title, body, sources, notes: '', ...extra,
});
const code = (title, path, start, end, body, sources) =>
  page(title, body, sources, {type:'code', code:{path,start,end}});
const lesson = (metadata, pages) => ({...metadata, pages:pages.map((item,index) => ({id:`${metadata.id}-${String(index+1).padStart(2,'0')}`, ...item}))});

const chapters = [
  {id:'C1',title:'실행 구조와 코드 읽기의 출발점',
    purpose:'차가 움직이지 않을 때 어느 프로그램의 어느 책임부터 확인할지 판단하기 위해 전체 왕복과 실행 단위를 배웁니다.',
    prerequisite:'Unreal의 Play와 별도 서버 실행이 서로 다른 작업이라는 경험만 있으면 됩니다. C++ 문법은 다음 장에서 배웁니다.',
    objectives:['운전 요청과 계산된 차량 상태의 책임을 구분합니다.','프로세스, 이벤트 루프, 콜백이 실제 main에서 어떤 관계인지 설명합니다.'],
    outcome:'입력 한 개의 왕복을 말로 설명하고, 함수의 입력·실패·출력을 찾아 읽을 수 있습니다.',
    next:'프로그램 사이의 경계를 알았으므로 한 프로그램 안에서 값과 수명을 전달하는 C++ 문법을 배웁니다.'},
  {id:'C2',title:'값을 안전하게 전달하는 C++',
    purpose:'참조와 포인터를 단순 문법으로 외우지 않고, 누가 값을 소유하며 언제 사용할 수 있는지 판단하도록 합니다.',
    prerequisite:'C1의 함수 입력·출력 구분과 프로세스/스레드 개념을 알고 있어야 합니다.',
    objectives:['const 참조·포인터·optional이 표현하는 서로 다른 약속을 읽습니다.','빌린 메모리의 수명과 공유 입력을 잠그는 범위를 설명합니다.'],
    outcome:'값 없음, 잘못된 수명, 동시 접근을 각각 다른 실패 원인으로 구분합니다.',
    next:'메모리의 값을 다른 프로세스에 전달하려면 공통 바이트 표현이 필요하므로 통신 계층을 배웁니다.'},
  {id:'C3',title:'통신 통로와 메시지 계약',
    purpose:'연결은 됐는데 조작이 안 되는 상황을 통로·메시지 경계·데이터 해석·입력 수락 단계로 나누어 이해합니다.',
    prerequisite:'C2의 객체 수명과 입력/출력 개념을 사용합니다. 네트워크 사전 지식은 필요하지 않습니다.',
    objectives:['TCP, WebSocket, Protobuf가 각각 해결하는 문제를 설명합니다.','스키마의 타입·필드 번호·단위와 생성 코드의 역할을 구분합니다.'],
    outcome:'vehicle.proto를 읽고 서버 생성 클래스와 Unreal 자체 파서를 함께 찾아갈 수 있습니다.',
    next:'계약을 바이트로 표현하는 방법을 이해하기 위해 태그와 varint부터 직접 계산합니다.'},
  {id:'C4',title:'Protobuf 바이트의 실제 구조',
    purpose:'파서가 무엇을 읽고 어디서 실패하는지 이해하도록 실제 바이트를 필드 번호와 값으로 손계산합니다.',
    prerequisite:'C3의 스키마·타입·메시지 경계가 필요합니다. 2진수와 16진수는 예제에서 함께 풉니다.',
    objectives:['태그, varint, 고정 32비트 값을 실제 숫자로 해석합니다.','문자열과 중첩 메시지의 길이를 바이트 단위로 추적합니다.'],
    outcome:'10 96 01과 5A 05 15 00 00 00 3F를 다른 층의 필드로 분해할 수 있습니다.',
    next:'바이트 구조를 배웠으므로 목록·선택·부재처럼 스키마가 표현하는 의미를 배웁니다.'},
  {id:'C5',title:'목록, 존재 여부와 호환성',
    purpose:'바이트를 읽는 데 성공했어도 바퀴 위치나 기어 의미를 잘못 해석할 수 있는 이유를 학습합니다.',
    prerequisite:'C4의 필드 번호, wire type, LEN 중첩 구조를 알고 있어야 합니다.',
    objectives:['repeated·oneof·optional을 서로 다른 표현 수단으로 설명합니다.','기본값과 정보 부재를 구분하고 스키마 확장의 양쪽 변경 지점을 찾습니다.'],
    outcome:'바퀴 21번 필드와 위치값 3을 구분하고, gear 부재가 현재 Drive로 남는 이유를 설명합니다.',
    next:'유효한 값이라도 현재 세계와 제어권에 속해야 하므로 연결·Play·시간 검증으로 이어집니다.'},
  {id:'C6',title:'현재 입력을 안전하게 수락하는 조건',
    purpose:'과거 입력이나 끊어진 조종 연결이 차량을 계속 움직이지 않도록 하는 실제 제어 수락 정책을 이해합니다.',
    prerequisite:'C1의 왕복 구조와 C3~C5의 Envelope·파싱·존재 여부가 필요합니다. 시간 단위는 본문에서 환산합니다.',
    objectives:['Hello·지도·연결·Play·순번이 각각 막는 오류를 설명합니다.','ControlLease의 입력 지연 거부와 무입력 안전 정지, 연결 폐기를 시간 예제로 판정합니다.'],
    outcome:'연결 성공과 조작 가능을 구분하고, 실제 송수신 코드와 회귀검사로 판단 근거를 제시합니다.',
    next:'다음 권에서는 여기서 배운 계약을 전제로 Unreal 표시와 서버 시뮬레이션의 세부 동작을 읽습니다.'},
];

const lessons = [];
lessons.push(lesson({id:'L01',chapter:'C1',title:'W 입력 한 개의 왕복과 함수 읽기',covers:['V1-01','V1-02','V1-03','V1-04'],
  problem:'Unreal은 60fps인데 키 반응이 늦습니다. 화면 주사율만으로 서버 입력이 최신인지 판단할 수 있을까요?',
  why:'입력·계산·표시의 책임을 먼저 나누어야 카메라나 메시지 형식만 고치며 원인과 다른 곳을 수정하는 일을 줄일 수 있습니다.',
  goal:'운전 요청이 계산된 상태로 돌아오는 경로를 설명하고 GetLatestState의 실패와 출력 약속을 읽습니다.',
  prerequisite:'Play를 눌러 서버에 연결해 본 경험이 있으면 됩니다. 함수의 세부 문법은 이후 단원에서 다시 풉니다.',
  next:'함수들의 역할을 알았으므로 실제로 어느 프로그램과 실행 흐름이 이 함수들을 호출하는지 확인합니다.'},[
  page('화면은 부드러운데 조작은 늦은 상황',[
    '카메라가 부드럽게 움직여도 운전 입력이 서버에 늦게 도착하면 차는 늦게 반응합니다. 화면을 그리는 횟수와 서버가 최신 입력으로 계산하는 시점은 서로 다른 관찰값이기 때문입니다.',
    '이 프로젝트에서 Unreal은 스로틀과 조향이라는 운전 의도를 보냅니다. 서버는 그 의도를 수락한 뒤 힘과 차량 상태를 계산합니다. 따라서 W 입력을 보냈다는 사실만으로 차량 위치가 바뀌었다고 판단할 수 없습니다.',
    '문제를 읽을 때는 입력 생성, 서버 수락, 계산 결과 수신, 화면 표시를 나누어 질문합니다. 어느 단계까지 정상인지 확인하면 지연 원인을 좁힐 수 있습니다. 이 왕복이 앞으로 읽을 함수들의 위치를 정해 줍니다.'
  ],refs(1,3)),
  page('왕복 그림을 읽는 기준',[
    '왼쪽 Unreal은 운전 의도를 ControlCommand에 담아 보내고, 오른쪽 C++ 서버는 계산된 WorldState를 돌려줍니다. 화살표 방향은 데이터를 누가 만들고 누가 소비하는지 나타냅니다.',
    '화면 보간은 받은 상태 사이를 부드럽게 보여 주는 작업입니다. 서버의 권한 있는 위치는 서버 계산이 갱신합니다. 이 그림을 보고 입력 메시지와 결과 메시지를 서로 바꾸어 설명하지 않는지 확인하세요.'
  ],refs(3,4),{type:'picture',imageKey:'architecture'}),
  page('지면 자료와 접지 계산의 책임',[
    'Unreal이 지면을 측정한다는 말은 노면 위치와 높이 같은 자료를 만든다는 뜻입니다. 서버는 그 자료와 바퀴 위치를 사용하여 접촉을 판단하고 힘을 계산합니다. 자료 작성과 물리 계산은 연결되지만 서로 다른 책임입니다.',
    '현재 경로에서는 측정 자료를 MapPackage로 내보내고 서버의 GroundQuery가 질의합니다. 매 틱 서버가 Unreal에 레이를 요청한 뒤 응답을 기다리는 구조로 이해하면 실제 코드와 다른 흐름을 상상하게 됩니다.',
    '카메라 회전이 답답하면 표시 계층을 먼저 보고, 지면을 뚫으면 측정 자료와 서버 접촉 계산의 연결을 봅니다. 이렇게 관찰한 증상을 책임에 배치하는 이유는 관련 없는 부분까지 한꺼번에 고치지 않기 위해서입니다.'
  ],refs(4)),
  code('현재 상태를 요청하는 실제 함수',P.client,139,149,[
    'bool 반환값은 성공 여부이고, OutState와 OutStateAgeSeconds는 결과를 써 주는 통로입니다.',
    '143행의 실패 경로와 144~148행의 성공 경로를 나누어 읽습니다.'
  ],refs(2)),
  page('GetLatestState의 줄별 의미',[
    '139~141행은 호출자가 상태와 상태 나이를 받을 수 있다는 약속입니다. 두 출력 매개변수의 &는 호출자가 준비한 변수에 결과를 쓸 수 있게 합니다. 함수 뒤 const는 이 호출이 일반 멤버 상태를 바꾸지 않는다는 제한입니다.',
    '143행에서 아직 받은 상태가 없으면 즉시 false를 반환합니다. 이 경로는 출력 변수에 최신 결과를 쓰지 않습니다. 호출자가 이전 값이 남은 변수를 그대로 읽으면 없는 최신 상태를 있다고 착각할 수 있습니다.',
    '144행은 저장된 상태를 복사합니다. 145~147행은 현재 시각에서 마지막 수신 시각을 빼고 음수는 0으로 제한합니다. 148행의 true는 이 출력이 이번 성공 경로에서 채워졌다는 뜻이며, 차량이 움직였다는 뜻은 아닙니다.'
  ],refs(2)),
  page('숫자를 넣어 읽는 성공과 실패',[
    '교육용으로 마지막 상태 수신 시각을 10.0초, 현재 시각을 10.2초라고 두겠습니다. bHasState가 참이면 함수는 저장 상태를 복사하고 나이 0.2초를 돌려줍니다. 0.2초는 서버 계산 시간 자체가 아니라 수신 후 경과입니다.',
    '같은 숫자를 준비해도 bHasState가 거짓이면 143행에서 끝납니다. 출력 변수에 이전 호출의 속도 5가 남았더라도 이번 결과로 채택할 수 없습니다. 성공 여부를 먼저 확인해야 하는 이유가 이 실패 경로에 있습니다.',
    '긴 함수도 이 방법으로 읽습니다. 먼저 반환형을 보고, 결과를 쓰는 줄과 조기 반환을 찾은 뒤 필요한 호출만 따라갑니다. 이름을 모두 외우는 것보다 어떤 조건에서 어떤 값이 유효한지 설명하는 것이 우선입니다.'
  ],refs(2)),
  page('확인 문제: 상태의 의미',[
    'A. W를 눌러 ControlCommand를 보낸 직후 서버 위치도 반드시 바뀌었다고 말할 수 있나요? 계산과 수락 단계 중 필요한 단계를 적어 보세요.',
    'B. GetLatestState가 false를 반환했는데 OutState.Speed에 5가 남아 있습니다. HUD에 최신 속도 5로 표시해도 되는지 실패 경로를 근거로 설명하세요.',
    'C. 카메라 보간과 노면 접지 힘을 각각 어느 책임에 연결해야 하나요? 단순히 Unreal 또는 서버라고 답하지 말고 무엇을 만들거나 계산하는지 적으세요.'
  ],refs(2,3,4),{type:'quiz'}),
  page('해설: 유효한 결과를 고르는 기준',[
    'A의 답은 즉시 확정할 수 없다는 것입니다. 메시지가 도착한 뒤 서버가 현재 연결의 입력인지 검증해야 하고, 수락한 입력으로 물리 계산을 진행해야 합니다. 요청과 결과를 나누어야 통신 문제를 물리 문제로 오해하지 않습니다.',
    'B는 최신 상태로 표시하면 안 됩니다. 143행의 false 경로는 출력값을 채우지 않으므로 숫자 5는 이전 데이터일 수 있습니다. C에서 카메라는 화면 표시 책임이고 접지 힘은 측정 지면 자료를 소비하는 서버 계산 책임입니다.',
    '이제 왕복을 설명할 때 함수 이름만 나열하지 말고 각 함수가 받는 값과 보장하는 결과를 함께 말해 보세요. 다음 단원에서는 이 함수들이 실행되는 프로세스와 이벤트 루프를 구분하여 지연이 생길 수 있는 위치를 살펴봅니다.'
  ],refs(2,3,4),{type:'answer'}),
]));

lessons.push(lesson({id:'L02',chapter:'C1',title:'프로세스, 이벤트 루프와 콜백',covers:['V1-05','V1-06','V1-07'],
  problem:'비동기 통신을 썼는데 서버의 긴 계산 때문에 입력 처리도 늦어질 수 있습니다. 비동기와 병렬 실행이 같은 뜻일까요?',
  why:'실행 단위를 구분해야 다른 프로세스의 주소를 보내거나, 콜백 안의 긴 계산이 자동으로 다른 스레드에서 실행된다고 오해하지 않습니다.',
  goal:'현재 main의 io_context와 방송 콜백을 읽고 등록 시점·호출 시점·객체 수명을 구분합니다.',
  prerequisite:'L01의 요청과 결과, 함수 입력과 출력 개념을 사용합니다.',next:'실행 흐름을 알았으므로 함수가 받는 참조와 포인터가 어떤 값 전달 약속인지 배웁니다.'},[
  page('같은 PC에 있어도 다른 메모리',[
    'UnrealEditor와 simcore_publisher는 별도 프로세스입니다. 프로세스는 실행 중인 프로그램의 주소 공간과 자원을 가집니다. 서버 안의 차량 객체 주소를 Unreal에 보내도 같은 객체를 직접 읽을 수 있다는 보장이 없습니다.',
    '스레드는 한 프로세스 안에서 명령의 실행 순서를 진행하는 단위입니다. 같은 프로세스의 스레드는 메모리를 공유할 수 있지만, 동시에 값을 바꿀 때는 동기화가 필요합니다. 프로세스 간 통신과 공유 변수 접근은 다른 문제입니다.',
    '이 구분 때문에 현재 프로젝트는 차량 객체의 주소 대신 약속된 숫자와 문자열을 보냅니다. 클라이언트라는 말은 서비스를 이용하는 역할이고 스레드는 실행 단위입니다. 두 단어를 같은 뜻으로 쓰면 디버깅 위치도 혼동하게 됩니다.'
  ],refs(5)),
  page('비동기 호출이 해결하는 대기',[
    '동기 읽기는 필요한 결과가 나올 때까지 호출 흐름을 기다리게 할 수 있습니다. 비동기 읽기는 작업을 요청하고 완료 시 실행할 함수를 등록합니다. 결과가 준비될 때 이벤트 루프가 그 함수를 호출한다는 방식으로 대기를 다룹니다.',
    '이벤트 루프는 준비된 작업을 받아 실행하는 흐름입니다. 현재 main은 하나의 io_context를 만들고 ioc.run()을 호출합니다. 주 통신과 시뮬레이션 처리가 이 흐름을 공유하므로 한 콜백의 긴 계산은 다음 작업을 늦출 수 있습니다.',
    '따라서 비동기라는 이름만으로 CPU 계산이 다른 코어에 분산된다고 기대하면 안 됩니다. 지도 로더처럼 별도 작업 경로도 있지만, 준비한 지면을 물리에 적용하는 시점은 틱 경계입니다. 실제 실행 위치를 코드로 확인해야 합니다.'
  ],refs(6)),
  code('WorldState 방송 동작을 등록하는 코드',P.main,307,311,[
    'broadcast_world_state에 람다를 저장합니다. 이 줄을 실행할 때 아직 메시지를 방송하는 것은 아닙니다.',
    '대괄호는 주변 객체를 참조하는 방식을, 괄호는 나중에 전달받을 메시지를 선언합니다.'
  ],refs(7)),
  page('람다와 콜백의 줄별 의미',[
    '307행은 호출할 동작들을 담는 callbacks 객체를 준비합니다. 308행은 이름 없는 함수인 람다를 멤버에 저장합니다. ws_server와 ioc 앞의 &는 원래 객체를 참조하여 나중에 사용할 수 있게 하는 참조 캡처입니다.',
    '같은 줄의 const std::string& message는 콜백 실행 시 전달받는 메시지입니다. const는 이 참조를 통해 메시지를 바꾸지 않는 약속입니다. 309행이 실제 방송 호출이며, 등록 이후 호스트가 콜백을 호출해야 도달합니다.',
    '310행은 물리 재생 검사가 존재하고 완료됐다면 이벤트 루프를 멈춥니다. 모든 방송이 서버를 종료하는 조건은 아닙니다. 이 발췌를 읽을 때 참조 캡처한 객체가 콜백 실행 시에도 살아 있어야 한다는 수명 조건까지 확인해야 합니다.'
  ],refs(7)),
  code('이벤트 루프가 실행되는 자리',P.main,409,413,[
    '종료 신호가 왔을 때 실행할 콜백을 먼저 등록하고 ioc.run()으로 이벤트 처리를 시작합니다.',
    'io_context 생성과 run 호출은 서로 다른 작업입니다.'
  ],[ref(P.main,'main',409,413),...refs(6)]),
  page('교육용 시간 추적: 한 콜백이 오래 걸리면',[
    '교육용으로 틱 콜백이 시각 0ms에 시작하여 80ms 동안 계산한다고 가정합니다. 같은 이벤트 루프에서 20ms에 준비된 입력 수신 작업이 있어도, 실행 중인 콜백이 끝나야 다음 작업을 처리할 수 있는 상황이 생깁니다.',
    '이 사례에서 네트워크 연결이 유지된다는 사실은 입력 처리가 제때 실행된다는 증거가 아닙니다. 서버가 언제 읽고 언제 검증했는지까지 나누어야 합니다. 80ms라는 수치는 원리를 설명하기 위한 가정이며 현재 측정 결과는 아닙니다.',
    '주 실행 흐름을 오래 점유하지 않도록 작업량을 제한하는 이유가 여기에 있습니다. 다만 임의로 스레드를 늘리면 공유 상태의 안전 문제가 추가됩니다. 다음 C++ 단원에서 수명과 잠금 약속을 함께 배우는 이유도 이 연결에 있습니다.'
  ],refs(6)),
  page('확인 문제: 등록과 실행',[
    'A. callbacks.broadcast_world_state에 람다를 대입한 줄을 지나면 메시지가 이미 전송됐나요? 실제 방송 호출이 있는 줄과 그 줄에 도달할 조건을 적으세요.',
    'B. 두 프로그램이 같은 PC에서 실행 중입니다. 서버 객체의 주소를 64비트 정수로 보내면 Unreal에서 바로 역참조할 수 있나요?',
    'C. 비동기 수신을 사용하면서 같은 이벤트 루프에서 긴 계산을 실행하면 수신 콜백이 지연될 수 있나요? 비동기와 병렬 실행을 구분하여 설명하세요.'
  ],refs(5,6,7),{type:'quiz'}),
  page('해설: 실행 흐름과 수명 조건',[
    'A에서는 동작을 저장했을 뿐입니다. 호스트가 저장한 콜백을 호출하면 309행이 실행됩니다. 전달한 message를 사용하는 동안 그 문자열이 유효해야 하고, 캡처한 ws_server와 ioc도 사용 시점까지 살아 있어야 합니다.',
    'B는 주소 공간이 다른 프로세스이므로 허용되는 일반 통신 방식이 아닙니다. 의미 있는 값을 직렬화해야 합니다. C는 가능합니다. 같은 실행 흐름을 점유하는 계산은 이미 준비된 다음 작업의 실행도 늦출 수 있습니다.',
    '이제 함수가 언제 실행되는지와 어느 객체를 사용하는지를 따로 설명할 수 있어야 합니다. 다음 단원에서 헤더 선언을 읽을 때 &나 *가 보이면 단순 기호 이름보다 값 복사 여부와 변경 권한, 유효성 조건을 먼저 질문해 보세요.'
  ],refs(5,6,7),{type:'answer'}),
]));

lessons.push(lesson({id:'L03',chapter:'C2',title:'선언, 참조, 포인터와 값 없음',covers:['V1-08','V1-09','V1-10','V1-11','V1-12'],
  problem:'함수가 값을 복사하는지, 호출자의 변수를 바꾸는지, 결과가 없을 수 있는지 선언만 보고 알 수 있어야 합니다.',
  why:'출력 포인터를 무조건 사용하거나 지면 질의 실패를 높이 0으로 바꾸면 유효하지 않은 정보를 실제 상태처럼 계산하게 됩니다.',
  goal:'헤더와 구현을 연결하여 const 참조·포인터·optional의 목적과 실패 처리를 실제 코드에서 설명합니다.',
  prerequisite:'L01의 성공 반환과 L02의 메모리/수명 개념을 사용합니다.',next:'값 전달 약속을 배운 다음에는 빌린 메모리의 수명과 동시에 접근하는 공유 입력을 살펴봅니다.'},[
  page('함수 선언이 먼저 필요한 이유',[
    '어떤 함수를 호출하려면 어떤 값을 넣고 어떤 결과를 받을지 알아야 합니다. 헤더는 호출자가 알아야 할 타입과 함수 선언을 제공합니다. 구현 파일은 그 약속을 실제 조건 검사와 계산으로 수행하는 본문을 담습니다.',
    'SimulationHost의 헤더에서 handle_client_message는 문자열 범위, 연결 세대, 수신 시각을 받습니다. 호출자가 이 정보를 제공하는 이유는 같은 바이트라도 어느 연결에서 언제 왔는지에 따라 수락 결과가 달라지기 때문입니다.',
    '헤더를 읽은 뒤 이름으로 구현을 찾으면 긴 파일도 목적을 잃지 않고 읽을 수 있습니다. 반환값과 매개변수의 역할을 한 문장으로 적고, 구현에서 그 약속을 지키는 성공 경로와 실패 경로를 각각 찾아보세요.'
  ],refs(8)),
  code('메시지 처리 함수의 헤더 선언',P.host,132,137,[
    'start와 stop은 반환값이 없는 함수입니다. handle_client_message는 처리 결과를 돌려줍니다.',
    '기본 인수가 적혀 있어도 실제 호출 경로가 어떤 값을 넘기는지 따로 확인해야 합니다.'
  ],refs(8)),
  page('헤더 발췌의 읽기 순서',[
    '132~133행의 void는 함수가 결과값을 반환하지 않는다는 뜻입니다. 134행의 ClientMessageResult는 메시지 처리를 어떤 결과로 끝냈는지 표현합니다. 반환형을 보면 호출자가 무엇을 검사해야 하는지 예상할 수 있습니다.',
    '135행의 string_view는 메시지의 메모리를 바라보는 범위입니다. 이 선언만으로 바이트의 소유권을 얻지는 않습니다. 136행은 연결 세대를 받고, 137행은 서버 수신 시각을 받습니다. 둘 다 메시지 내용 외의 실행 맥락입니다.',
    '기본 연결 세대 0과 기본 현재 시각은 인수를 생략한 호출에 적용됩니다. 이것만 보고 생산 통신 경로가 식별과 시간 검사를 생략한다고 결론 내리면 안 됩니다. 구현과 실제 호출자가 명시적으로 넘기는 값을 함께 확인합니다.'
  ],refs(8)),
  code('읽기 전용 참조를 내부 값으로 복사',P.physics,629,637,[
    '호출자의 input을 const 참조로 읽고, 내부 input_에 복사한 뒤 정상 범위로 제한합니다.',
    '이 const는 함수 전체나 input_의 불변을 뜻하지 않습니다.'
  ],refs(9)),
  page('const와 복사가 맡는 서로 다른 역할',[
    '629행의 const VehicleInput&는 호출자가 준 객체를 읽는 통로입니다. 참조를 받으므로 매개변수를 만들 때 전체 입력을 복사할 필요가 없고, const를 붙여 이 통로를 통해 원본을 바꾸지 않겠다고 선언합니다.',
    '630행은 공유 입력 저장소를 잠급니다. 631행의 input_ = input은 내부 보관용 복사입니다. 함수가 반환한 뒤에도 다음 물리 틱에서 입력을 사용해야 하므로 호출자의 임시 객체 수명에 계속 의존하지 않도록 보관합니다.',
    '632~637행은 유한한 숫자인지 확인한 뒤 스로틀·브레이크는 0~1, 조향은 -1~1로 제한합니다. 예를 들어 스로틀 1.2는 내부에서 1이 됩니다. const가 있어도 내부 복사본을 정리할 수 있다는 점을 코드가 직접 보여 줍니다.'
  ],refs(9)),
  code('선택적인 오류 출력 포인터',P.decoder,21,26,[
    'error는 오류 문장을 받을 문자열을 가리킵니다. 출력이 필요 없다면 호출자가 nullptr를 줄 수 있습니다.',
    'if (error)는 포인터 유무를 확인한 뒤 역참조하도록 합니다.'
  ],refs(10)),
  page('포인터 검사와 optional의 목적',[
    '21행의 std::string*는 문자열 객체를 가리키는 포인터입니다. 23행이 포인터가 없는 경우를 걸러 주고, 24행의 *error가 실제 출력 객체를 선택합니다. nullptr 검사만으로 이미 파괴된 객체의 수명까지 증명할 수는 없습니다.',
    'optional<T>는 결과 T가 있는 상태와 없는 상태를 한 반환형에 담습니다. 예를 들어 지면 높이 0m는 유효한 결과일 수 있으므로, 지면을 못 찾았다는 실패를 숫자 0과 같은 값으로 표현하면 둘을 구분할 수 없게 됩니다.',
    '포인터가 선택적 출력 통로라면 optional은 결과의 존재 여부를 표현합니다. 이 목적 차이를 기억하면 왜 오류 문장은 포인터로 받고 지면 질의는 optional을 반환하는지 설명할 수 있습니다. 둘 다 없는 경우의 처리 정책이 필요합니다.'
  ],refs(10,11)),
  code('지면을 찾지 못한 실제 반환',P.ground,94,102,[
    '이 발췌는 FlatGroundQuery의 평지 질의입니다. 전체 지도 탐색 구현과 구분해서 읽습니다.',
    '질의 시작점 아래의 거리 범위에 평지가 있어야 GroundHit를 반환합니다.'
  ],refs(12)),
  page('지면 질의에 숫자를 넣는 과정',[
    '94행은 시작 높이에서 평지 높이를 빼 거리로 만듭니다. 교육용으로 시작점 1.5m, 평지 0.2m이면 아래 거리 1.3m입니다. 최대 질의 거리 2m라면 범위 안이므로 99~102행에서 위치·법선·거리를 돌려줍니다.',
    '같은 시작점에서 최대 거리를 1m로 줄이면 95행 조건이 참이고 96행의 nullopt로 끝납니다. 평지가 시작점보다 위에 있어 거리 자체가 음수인 경우도 실패합니다. 실패는 아래 방향 질의가 그 지면을 찾지 못했다는 뜻입니다.',
    '이때 호출자가 높이 0m를 임의로 만들면 원래 존재하지 않던 지지를 계산에 넣게 됩니다. 반대로 실제 높이 0m를 실패로 버려도 잘못입니다. 반환값의 존재를 검사한 뒤 성공한 GroundHit만 계산에 사용하는 이유가 여기에 있습니다.'
  ],refs(11,12)),
  page('확인 문제: 기호보다 약속',[
    'A. const VehicleInput& input을 받은 함수 안에서 input_ = input을 실행할 수 있는 이유를 원본과 내부 복사본으로 나누어 설명하세요.',
    'B. error가 nullptr일 때 set_error는 어느 줄에서 문자열 대입을 건너뛰나요? nullptr가 아니라는 사실이 객체 수명까지 보장하나요?',
    'C. 시작 높이 2m, 평지 높이 0m, 최대 질의 거리 1m입니다. 함수 결과와 그 이유를 적고, 이 결과를 높이 0으로 대체할 때 생기는 문제를 설명하세요.'
  ],refs(9,10,12),{type:'quiz'}),
  page('해설: 변경 권한과 결과 유효성',[
    'A의 const는 input 참조를 통한 원본 변경을 제한합니다. input_는 별도의 내부 객체이므로 복사하고 범위를 제한할 수 있습니다. 선언의 const 위치와 실제 대입 대상이 무엇인지 구분해야 코드 전체가 불변이라고 오해하지 않습니다.',
    'B는 23행의 조건이 거짓이므로 24행을 건너뜁니다. 주소가 남아 있어도 대상이 이미 파괴됐으면 안전하지 않습니다. C는 거리 2m가 최대 1m를 넘으므로 nullopt입니다. 평지 높이가 0이라는 사실과 질의 성공 여부는 별개입니다.',
    '헤더를 다시 볼 때 어떤 인수는 읽기 전용이고 어떤 통로는 출력용인지 표시해 보세요. 다음 단원은 값의 주소를 갖고 있는 것과 값을 안전하게 사용할 수 있는 것이 왜 다른지, 수명과 공유 접근의 관점에서 이어집니다.'
  ],refs(8,9,10,11,12),{type:'answer'}),
]));

lessons.push(lesson({id:'L04',chapter:'C2',title:'빌린 메모리의 수명과 공유 입력 잠금',covers:['V1-13','V1-14','V1-15','V1-16','V1-17'],
  problem:'메시지 뷰를 저장했는데 원본 버퍼가 사라지거나, 입력을 읽는 도중 다른 실행 흐름이 값을 바꾸면 어떻게 될까요?',
  why:'값의 존재 여부와 함께 사용 시점의 수명·동시 접근을 확인해야 읽기 전용이라는 이유만으로 안전하다고 착각하지 않습니다.',
  goal:'뷰·소유권·참조 캡처의 차이와 mutex/RAII가 보호하는 정확한 범위를 설명합니다.',
  prerequisite:'L02의 스레드와 콜백, L03의 참조·포인터를 사용합니다.',next:'메모리 내부의 안전한 전달을 알았으므로 다른 프로세스에 바이트를 보내는 통신 계층을 배웁니다.'},[
  page('읽기 전용이어도 사라질 수 있는 메모리',[
    'string_view와 TArrayView는 원본 메모리의 위치와 범위를 바라보는 가벼운 객체입니다. 뷰 자체를 복사해도 원본 바이트 전체가 새로 생기지는 않습니다. 따라서 뷰를 오래 저장할수록 원본 버퍼의 수명을 확인해야 합니다.',
    'const는 그 통로로 값을 바꾸지 않는 제한입니다. 원본 객체를 자동으로 살아 있게 만드는 약속은 아닙니다. 예를 들어 지역 문자열을 가리키는 뷰를 저장한 뒤 함수가 끝나 문자열이 사라지면 이후 읽기는 안전하지 않습니다.',
    '현재 메시지 처리와 자체 파서는 이런 뷰를 사용하므로, 파싱이 끝나는 범위와 바이트 보관 범위를 구분해서 읽어야 합니다. 나중에도 값을 써야 한다면 살아 있는 소유 객체를 유지하거나 필요한 데이터를 복사하는 방법이 필요합니다.'
  ],refs(15)),
  page('값 복사와 빌린 메모리의 비유',[
    '그림의 Copy는 값을 별도 객체에 보관하고, Reference는 같은 객체를 다른 이름으로 접근하는 차이를 보여 줍니다. 포인터를 포함한 객체를 복사한다고 가리킨 데이터까지 항상 깊게 복사되는 것은 아닙니다.',
    'View는 원본의 일정 범위를 빌려 보는 통로입니다. 원본 문서가 사라지면 빌린 범위도 유효하지 않다는 비유로 읽습니다. const나 뷰의 복사가 원본 수명까지 자동으로 늘려 주지 않는다는 점이 핵심입니다.'
  ],refs(15),{type:'picture',imageKey:'cpp_memory'}),
  page('소유 객체와 빌린 접근 경로',[
    'unique_ptr는 한 객체를 소유하며 정상적인 수명 종료 때 해제할 책임을 표현합니다. 참조와 포인터, 뷰는 보통 이미 존재하는 객체에 접근하는 경로입니다. 접근할 수 있다는 사실과 해제 책임을 갖는 것은 서로 다른 약속입니다.',
    '앞에서 본 방송 람다는 ws_server를 참조 캡처했습니다. 이 캡처가 서버 객체를 새로 만들거나 수명을 늘려 주지는 않습니다. 콜백을 보관하는 곳과 캡처 대상이 사라지는 시점을 함께 확인해야 나중의 호출이 안전합니다.',
    '메모리 문제를 읽을 때는 값을 누가 만들었는지, 누가 보관하는지, 마지막 사용이 언제인지 순서대로 적어 보세요. 이 세 질문은 네트워크 비동기 콜백처럼 등록 시점과 사용 시점이 떨어진 코드에서 특히 중요합니다.'
  ],refs(7,15)),
  page('공유 입력에 mutex가 필요한 이유',[
    '입력을 쓰는 흐름과 물리 틱이 입력을 읽는 흐름이 같은 저장소에 접근할 수 있습니다. 읽는 도중 값이 바뀌면 스로틀은 새 입력이고 브레이크는 이전 입력인 불일치 상태를 관찰할 수 있으므로 접근을 조정해야 합니다.',
    'mutex는 보호 대상에 접근하는 실행 흐름이 같은 잠금 규칙을 지키도록 하는 도구입니다. 잠금을 잡고 공유 데이터를 읽거나 쓰는 구간을 임계 구역이라고 부릅니다. 잠금은 선언만으로 효과가 생기지 않고 실제 접근 경로가 따라야 합니다.',
    '공유 입력을 짧게 복사한 뒤 긴 물리 계산은 복사본으로 진행하면 잠금을 오래 잡을 필요가 줄어듭니다. 잠금이 오래 유지되면 입력을 쓰려는 쪽도 기다릴 수 있기 때문에 정확한 범위와 수명을 함께 설계합니다.'
  ],refs(13,14)),
  code('틱 시작에서 입력을 복사하는 코드',P.physics,685,692,[
    '안쪽 블록은 공유 input_을 지역 변수 in에 복사하는 동안만 잠금을 유지합니다.',
    '블록 뒤의 물리 계산은 잠금 객체가 사라진 뒤 실행됩니다.'
  ],refs(14)),
  page('RAII가 잠금 누락을 줄이는 원리',[
    '685행은 이번 틱이 사용할 지역 입력 in을 만듭니다. 686행에서 작은 블록을 열고, 687행의 lock_guard 생성자가 mutex를 잠급니다. 688행은 공유 input_을 in으로 복사하므로 이번 틱에 사용할 한 묶음의 입력을 확보합니다.',
    '689행에서 블록이 끝나면 lock 객체의 수명이 끝나고 소멸자가 잠금을 해제합니다. 자원의 획득과 해제를 객체 수명에 연결하는 방식을 RAII라고 부릅니다. 사람이 모든 정상 반환 위치에 해제 호출을 반복할 필요를 줄입니다.',
    '691행 이후 계산은 지역 복사본을 쓸 수 있습니다. 이 구조의 목적은 계산 전체를 잠그는 것이 아니라 공유 입력을 안전하게 얻는 데 있습니다. RAII가 있다고 다른 공유 객체나 잘못된 수명까지 자동으로 안전해지는 것은 아닙니다.'
  ],refs(14)),
  page('입력 변경 순서를 따라가는 예',[
    '교육용으로 공유 입력의 스로틀이 0.2라고 가정합니다. 물리 틱이 잠금을 잡고 in에 0.2를 복사한 뒤 잠금을 풉니다. 이후 다른 흐름이 공유 입력을 0.8로 바꾸어도 이미 복사한 이번 틱의 in은 0.2를 유지합니다.',
    '다음 틱이 다시 잠금을 잡고 복사하면 0.8을 얻습니다. 한 틱 안에서 필요한 입력이 갑자기 바뀌지 않도록 시작 시점의 묶음을 사용하는 것입니다. 정확히 어느 틱부터 변경을 반영하는지는 실제 호출 순서와 실행 시점에 달려 있습니다.',
    '반면 지역 뷰만 복사했다면 원본 값까지 독립적으로 복사한 것이 아닙니다. 값 복사와 뷰 복사를 같은 행동으로 읽으면 이 예를 잘못 예측하게 됩니다. 타입과 대입 대상을 먼저 확인해야 하는 이유가 다시 드러납니다.'
  ],refs(14,15)),
  page('확인 문제: 수명과 잠금',[
    'A. 읽기 전용 뷰를 멤버에 저장하면 원본 지역 문자열이 사라져도 안전한가요? const와 소유권이 각각 보장하는 것을 적으세요.',
    'B. 앞의 코드에서 잠금은 692행의 지면 지원 갱신까지 유지되나요? lock 객체가 생성되고 사라지는 줄을 근거로 답하세요.',
    'C. 스로틀 0.2를 지역 in에 복사한 직후 공유 input_이 0.8로 바뀌면 이미 복사한 in은 어떻게 되나요? 뷰 복사였다면 왜 다시 확인해야 하나요?'
  ],refs(14,15,16),{type:'quiz'}),
  page('해설: 어디까지 안전해지는가',[
    'A는 안전하지 않습니다. const는 수정 경로를 제한하지만 원본 수명을 연장하지 않습니다. 원본을 보관하는 소유 객체가 있어야 나중의 읽기를 보장할 수 있습니다. 람다의 참조 캡처도 같은 수명 질문이 필요합니다.',
    'B는 687행에서 생성한 lock이 689행에서 사라지므로 그 뒤 계산까지 잠그지 않습니다. C에서 VehicleInput을 값으로 복사한 in은 0.2를 유지합니다. 뷰를 복사했다면 원본 저장소를 계속 바라보므로 같은 결론을 바로 낼 수 없습니다.',
    '이 단원까지 C++를 읽는 기준은 변경 권한, 값의 존재, 소유권, 접근 시점입니다. 다음에는 이 메모리 구조를 그대로 다른 프로그램에 복사할 수 없는 이유를 바탕으로 TCP와 WebSocket, Protobuf의 역할을 나누어 보겠습니다.'
  ],refs(14,15,17),{type:'answer'}),
]));

lessons.push(lesson({id:'L05',chapter:'C3',title:'TCP, WebSocket과 직렬화의 역할',covers:['V1-18','V1-19','V1-20'],
  problem:'연결 로그는 정상인데 메시지를 해석하지 못합니다. TCP가 바이트를 전달했다는 사실만으로 운전 데이터가 완성됐다고 볼 수 있을까요?',
  why:'통신 통로와 데이터 형식을 분리해야 프레임 분할 문제를 Protobuf 필드 문제로 오해하지 않습니다.',
  goal:'통로·메시지 경계·직렬화·수락 정책을 구분하고 실제 분할 수신 코드를 추적합니다.',
  prerequisite:'C2의 바이트 소유와 뷰 개념을 사용합니다. 네트워크 계층의 이름은 이 단원에서 정의합니다.',next:'공통 바이트 표현이 왜 필요한지 이해했으므로 그 약속을 선언하는 vehicle.proto를 읽습니다.'},[
  page('연결 성공 뒤에도 남는 질문',[
    '두 프로그램이 TCP로 연결되면 순서 있는 바이트 흐름을 주고받을 수 있습니다. 그러나 바이트 흐름 자체는 어디까지가 차량 상태 한 개인지, 어떤 숫자가 스로틀인지 알려 주지 않습니다. 각 문제를 해결하는 약속이 추가로 필요합니다.',
    'WebSocket은 메시지를 구분하는 통신 규칙을 제공합니다. 이 프로젝트는 그 메시지의 내용을 바이너리로 보냅니다. Protobuf는 그 내부 바이트를 필드와 값으로 표현하는 규칙이며, 실제 메시지를 전달하는 소켓을 대신 열지는 않습니다.',
    '마지막으로 애플리케이션은 해석한 명령을 받아도 되는지 검사합니다. 연결이 정상이고 바이트도 파싱됐어도 지도나 현재 연결 식별이 다르면 거부할 수 있습니다. 실패한 층을 나누는 것이 디버깅의 첫 질문이 됩니다.'
  ],refs(18)),
  page('계층마다 다른 확인 대상',[
    '예를 들어 스로틀 0.5를 보낼 때 TCP는 바이트 전달을 담당하고 WebSocket은 바이너리 메시지의 경계를 담당합니다. Protobuf 파서는 해당 바이트에서 ControlCommand의 throttle이라는 값을 읽을 수 있게 합니다.',
    '서버는 그 값이 현재 제어 연결에서 왔는지와 최신 입력인지 다시 검사합니다. 따라서 연결 재시도, 메시지 조립, 필드 해석, 제어권 확인은 서로 다른 근거를 봐야 합니다. 모든 오류를 연결 실패 한 종류로 묶으면 원인을 놓칩니다.',
    '현재 왕복의 메시지 내용은 Protobuf 바이너리입니다. 스키마 파일 전체를 매 패킷에 붙여 보내는 방식은 아닙니다. 송신자와 수신자가 같은 필드 의미를 미리 알고 있어야 하므로 다음 단원에서 그 계약 파일을 함께 읽습니다.'
  ],refs(18,20)),
  page('통신 계층 그림의 읽는 순서',[
    'TCP는 순서 있는 바이트 전달을, WebSocket은 메시지 경계를 담당합니다. 내부 Protobuf 바이트를 읽으면 throttle 같은 의미 있는 필드를 얻습니다. 각 층은 서로 다른 질문을 해결하므로 실패 원인도 나누어 확인합니다.',
    'Schema는 양쪽이 미리 공유하는 해석 계약입니다. schema 파일 전체를 매번 통신 패킷 안에 넣어 보낸다는 뜻으로 읽지 마세요. 현재 운전 메시지는 필드 태그와 값 바이트를 담고 서버는 그 값을 별도 조건으로 수락합니다.'
  ],refs(18,20),{type:'picture',imageKey:'packet_layer'}),
  page('한 메시지가 나누어 도착하는 경우',[
    '소켓 이벤트 한 번을 곧 완성 메시지 한 개라고 가정하면 분할 수신에서 실패할 수 있습니다. 조각의 끝과 논리적인 메시지의 끝을 구분해야 하므로 현재 Unreal 수신 경로는 완료 여부를 확인하며 바이트를 모읍니다.',
    '교육용으로 메시지 300바이트가 120바이트와 180바이트 조각으로 들어온다고 가정합니다. 첫 조각만 파서에 넘기면 뒤의 필드나 길이 범위가 잘린 상태일 수 있습니다. 마지막 조각이 온 뒤 전체 300바이트를 해석해야 합니다.',
    '조립 버퍼에는 크기 한도도 필요합니다. 끝나지 않는 데이터나 지나치게 큰 메시지를 계속 모으면 메모리를 소모하기 때문입니다. 현재 수신 한도는 1MiB이며 초과 경로는 누적 메시지를 버리고 정상 표시 갱신에 사용하지 않습니다.'
  ],refs(19)),
  code('완성된 메시지만 다음 단계로 전달',P.client,940,949,[
    '940행에서 조각을 누적한 뒤, 마지막 조각인지 검사합니다.',
    'CompleteMessage를 만든 다음 Hello 여부를 포함한 메시지 해석을 시작합니다.'
  ],[ref(P.client,'USimCoreClientComponent::ApplyBinaryMessage',940,949),...refs(19)]),
  page('분할 수신 코드의 줄별 해설',[
    '940행의 Append는 새 조각을 기존 누적 바이트 뒤에 붙입니다. 941행은 마지막 조각이 아니면 반환하므로 아직 완성되지 않은 내용을 정상 메시지로 처리하지 않습니다. 이것이 조각과 메시지를 구분하는 실행 조건입니다.',
    '943행의 MoveTemp는 누적 배열의 저장 내용을 완성 메시지 쪽으로 옮길 수 있게 합니다. 944행에서 다음 메시지에 쓸 누적 상태를 초기화합니다. 여기서 값 소유를 옮기는 흐름은 원본을 계속 바라보는 뷰 복사와 다릅니다.',
    '945~949행은 해석에 필요한 임시 값과 오류 문자열을 준비하고 Hello Envelope 파서를 호출합니다. 이 단계까지 왔다는 것은 메시지 조립을 마쳤다는 뜻입니다. 이후 파싱과 지도·세션 검사가 모두 성공했다는 뜻은 아직 아닙니다.'
  ],[ref(P.client,'USimCoreClientComponent::ApplyBinaryMessage',940,949)]),
  page('객체를 바이트로 바꾸는 목적',[
    '직렬화는 의미 있는 값을 정해진 바이트 표현으로 바꾸는 일입니다. 역직렬화는 바이트를 읽어 메시지 값으로 복원하는 일입니다. 송신 측 C++ 객체와 수신 측 Unreal 구조체의 메모리 배치가 같지 않아도 값을 교환할 수 있습니다.',
    '구조체 메모리를 그대로 복사하면 패딩, 포인터 주소, 타입 크기와 수명까지 상대가 같다고 가정하게 됩니다. 포인터가 가리킨 데이터까지 자동으로 전송되는 것도 아닙니다. 공통 직렬화 규칙을 사용하는 이유는 이런 구현 차이를 분리하기 위해서입니다.',
    '현재 서버는 생성된 Protobuf 클래스를 사용하고 Unreal은 자체 파서를 사용합니다. 구현 도구는 다르지만 필드 번호와 wire 형식, 물리 의미의 계약은 맞아야 합니다. 다음 단원은 그 계약을 사람이 읽는 선언에서 출발합니다.'
  ],refs(20)),
  page('확인 문제: 실패한 층 찾기',[
    'A. 300바이트 메시지의 첫 120바이트를 받았습니다. 바이트가 있다는 이유로 완성 상태를 표시하면 어떤 문제가 생길 수 있나요?',
    'B. TCP가 순서를 보장하면 스로틀 필드의 단위와 현재 제어권까지 알 수 있나요? 각각 어느 계약을 확인해야 하는지 적으세요.',
    'C. 문자열 포인터가 든 C++ 구조체를 통째로 복사해 보내면 수신자가 문자열 내용까지 얻나요? L04의 소유권과 프로세스 주소 공간을 함께 사용하여 설명하세요.'
  ],refs(18,19,20),{type:'quiz'}),
  page('해설: 통로와 의미를 구분하는 이유',[
    'A는 아직 잘린 메시지일 수 있습니다. 현재 코드는 마지막 조각까지 모은 뒤 파싱을 시작하므로 부분 수신을 완성 상태로 갱신하지 않습니다. 크기 제한은 파싱 전에 누적 버퍼가 무한히 커지는 위험도 제한합니다.',
    'B에서 TCP 순서는 데이터 전달의 약속입니다. 스로틀 의미는 스키마와 애플리케이션 계약을, 제어권은 현재 세션 검사를 봐야 합니다. C의 포인터는 송신 프로세스의 주소이므로 문자열 내용을 자동으로 제공하지 않습니다.',
    '이제 연결 정상이라는 증거가 어느 단계까지만 설명하는지 말할 수 있어야 합니다. 다음 단원에서는 스키마가 스로틀과 위치를 어떻게 선언하는지, 같은 선언을 서버 생성 코드와 Unreal 수동 코드가 어떻게 소비하는지 읽습니다.'
  ],refs(18,19,20),{type:'answer'}),
]));

lessons.push(lesson({id:'L06',chapter:'C3',title:'IDL, 생성 코드와 물리 단위 계약',covers:['V1-21','V1-22','V1-23','V1-24','V1-25','V1-26'],
  problem:'양쪽 모두 float를 읽는데도 속도나 회전이 틀릴 수 있습니다. 타입이 같다는 사실은 단위와 방향까지 같다는 뜻일까요?',
  why:'필드 번호와 저장 타입뿐 아니라 실제 의미를 함께 읽어야 컴파일·파싱 성공 뒤에 남는 단위 오류를 찾습니다.',
  goal:'vehicle.proto와 protoc의 관계, 생성 클래스/자체 파서의 책임, ControlCommand/EntityState의 단위 차이를 설명합니다.',
  prerequisite:'L05의 직렬화 목적과 메시지 계층을 알고 있어야 합니다.',next:'계약의 선언을 읽었으므로 다음에는 그 선언이 실제 태그와 바이트가 되는 과정을 계산합니다.'},[
  page('공유 계약을 파일에 적는 이유',[
    '서버는 어떤 숫자를 위치로 쓰는데 클라이언트는 속도로 읽는다면 바이트가 정상이어도 동작은 틀립니다. 두 프로그램이 공유할 데이터 모양과 식별 번호를 한 선언으로 적어 두면 서로 다른 구현이 같은 기준을 확인할 수 있습니다.',
    'IDL은 프로그램 사이의 데이터나 인터페이스 약속을 적는 언어라는 뜻입니다. 이 프로젝트의 vehicle.proto는 Protobuf 스키마이며 메시지 이름, 각 필드의 타입과 번호를 선언합니다. 번호는 초기값이 아니라 바이트 해석에 쓰는 식별자입니다.',
    '스키마는 읽을 데이터의 형태를 알려 주지만 물리 계산식 전체를 만들지는 않습니다. 단위와 좌표 방향은 주석과 변환 코드까지 함께 봐야 합니다. 선언을 읽는 목적은 자동 생성의 편리함과 의미 계약의 책임을 동시에 이해하는 데 있습니다.'
  ],refs(21,26)),
  page('공통 스키마로 값의 의미를 복원',[
    '왼쪽 객체의 값을 직렬화하면 가운데 바이트가 되고, 오른쪽은 같은 스키마 계약으로 그 바이트를 해석합니다. 서로 다른 메모리 배치를 가진 프로그램도 필드 번호와 타입을 맞추어 의미 있는 값을 주고받을 수 있습니다.',
    '위쪽 Schema는 Serialize와 Parse가 따라야 할 약속입니다. 스키마 문서 자체를 차량 상태에 매번 실어 보내는 그림이 아닙니다. 현재 서버는 생성 클래스를, Unreal은 같은 계약에 맞춘 자체 파서를 사용합니다.'
  ],refs(20,21,23),{type:'picture',imageKey:'protobuf'}),
  code('Vector3d의 실제 스키마',P.proto,46,50,[
    'x, y, z는 double 타입이고 오른쪽 1, 2, 3은 필드 번호입니다.',
    '벡터가 위치인지 속도인지에 따라 의미와 단위는 사용하는 필드의 계약에서 정합니다.'
  ],refs(21)),
  page('선언의 숫자를 읽는 법',[
    '46행은 Vector3d라는 중첩 가능한 메시지를 시작합니다. 47행의 double x = 1은 x라는 이름의 값을 1번 필드로 식별한다는 뜻입니다. x의 값이 언제나 1이라는 뜻으로 읽으면 모든 이후 바이트 계산이 잘못됩니다.',
    '48~49행은 y와 z에 서로 다른 번호를 줍니다. 세 성분의 타입이 같아도 필드 번호로 어느 성분인지 구분합니다. 값 자체는 실행 중에 채우므로 교육용 위치 (2.0, 3.0, 0.5)를 동일한 선언으로 표현할 수 있습니다.',
    'Vector3d를 position_enu에 쓰면 동·북·위 방향의 미터 단위 위치입니다. 다른 필드에서 속도나 각속도를 담을 때는 다른 단위를 가질 수 있습니다. 같은 메시지 타입을 재사용하는 편리함 때문에 사용 지점의 설명도 반드시 읽어야 합니다.'
  ],refs(21,26)),
  code('protoc 생성이 연결된 CMake 설정',P.cmake,39,45,[
    'protobuf_generate가 스키마에서 C++ 결과를 만들도록 빌드에 연결합니다.',
    '생성 결과를 고치기보다 원본 계약과 변환 코드를 구분해서 관리해야 합니다.'
  ],refs(22)),
  page('생성 코드와 Unreal 구현의 차이',[
    '39행은 protoc의 메시지 클래스 생성을 연결합니다. TARGET은 빌드 대상을, PROTOS는 입력 스키마를 지정합니다. LANGUAGE cpp는 C++ 결과를 뜻하고 출력 경로는 현재 빌드 폴더입니다.',
    '서버는 생성 클래스의 ParseFromArray와 SerializeAsString 같은 기능을 사용합니다. Unreal은 현재 SimCoreProtocol의 자체 읽기·쓰기 코드를 사용합니다. 스키마를 바꿨다고 Unreal의 수동 분기와 화면 표시가 자동으로 늘어나지는 않습니다.',
    '따라서 새 필드는 선언, 서버 값 변환, Unreal 해석과 사용을 함께 확인합니다. 생성 파일은 계약의 결과이며 실제 차량 값을 채우는 일은 프로젝트 책임입니다. 생성 성공과 기능 완성을 구분해야 합니다.'
  ],refs(22,23)),
  code('Envelope가 감싸는 공통 정보',P.proto,277,280,[
    'Envelope는 실제 payload와 함께 메시지 순번, 계산 시간, 출처, 지도 식별을 담습니다.',
    '각 숫자를 왜 전달하는지는 연결과 제어권 단원에서 실제 거부 상황으로 확인합니다.'
  ],refs(24)),
  page('요청값과 계산값, 타입과 단위',[
    'ControlCommand의 throttle 0.5는 운전 요청입니다. 목표 위치나 속도를 직접 지정하지 않으므로 차가 몇 미터 움직일지는 서버 계산 결과입니다. EntityState의 speed는 그 결과를 m/s로 표현합니다. 둘의 역할을 바꾸어 읽지 마세요.',
    '속도 10m/s를 계기판 km/h로 표시하려면 3.6을 곱해 36km/h로 바꿉니다. 이것은 단위 환산 예입니다. float라는 타입은 두 숫자를 모두 저장할 수 있으므로 타입 검사만으로 단위 오류를 발견할 수 없습니다.',
    'heading은 북쪽 0도와 시계 방향 양수이며 yaw_rate는 왼쪽 양수인 rad/s입니다. steering 입력 -1~1과 실제 바퀴 각도 rad도 다릅니다. 정의를 적고 필요한 변환만 적용해야 방향과 크기를 보존합니다.'
  ],refs(25,26)),
  page('확인 문제: 선언과 실행값',[
    'A. double x = 1에서 숫자 1의 역할은 무엇인가요? 실제 위치 x=20m와 모순되는지 설명하세요.',
    'B. 서버 생성 코드 빌드가 성공했습니다. 새 필드를 Unreal 화면에서 사용하려면 어떤 수동 구현들을 더 확인해야 하나요?',
    'C. speed=10을 km/h 계기판에 그대로 쓰면 왜 틀리나요? heading, yaw_rate, steering_angle의 타입이 모두 float여도 그대로 섞으면 안 되는 이유도 적으세요.'
  ],refs(21,22,23,25,26),{type:'quiz'}),
  page('해설: 형식과 의미의 이중 계약',[
    'A의 1은 필드 번호입니다. 실제 값 20은 실행 중 그 필드에 저장하므로 모순이 없습니다. B는 Unreal 자체 파서의 필드 분기와 저장 구조, 화면 소비를 확인해야 합니다. 서버에서도 생성 클래스에 실제 값을 채우는 변환이 필요합니다.',
    'C의 speed 단위는 m/s이므로 10은 36km/h입니다. heading의 degree와 yaw_rate의 rad/s는 종류와 단위가 다르며 방향 부호도 따로 확인해야 합니다. 같은 float라는 이유로 값을 그대로 대입하면 의미를 잃을 수 있습니다.',
    '이 단원의 완료 기준은 선언을 보고 이름, 타입, 식별 번호, 실제 의미를 각각 말하는 것입니다. 다음 단원에서는 필드 번호와 저장 방식이 태그라는 정수로 합쳐지는 과정을 직접 계산하여 파서의 비트 연산을 읽습니다.'
  ],refs(21,23,26),{type:'answer'}),
]));

lessons.push(lesson({id:'L07',chapter:'C4',title:'태그와 varint를 손으로 해석하기',covers:['V1-27','V1-28','V1-29','V1-30','V1-31','V1-32'],
  problem:'파서는 숫자 이름이 없는 바이트에서 어느 필드이고 몇 바이트를 읽어야 하는지 알아야 합니다.',
  why:'태그와 varint의 구조를 알면 알 수 없는 필드를 건너뛰는 이유와 잘린 정수·범위 초과를 거부하는 이유를 코드로 설명할 수 있습니다.',
  goal:'필드 번호와 wire type을 태그로 계산하고 varint 150을 쓰기/읽기 코드와 대응합니다.',
  prerequisite:'L06의 필드 번호와 저장 타입 개념이 필요합니다.',next:'길이가 달라지는 정수를 배운 뒤 고정 32비트와 LEN을 비교합니다.'},[
  page('바이트에는 필드 이름이 반복되지 않습니다',[
    'Protobuf 바이트를 읽는 파서는 문자열 throttle을 매번 찾는 방식으로 동작하지 않습니다. 각 필드 앞의 태그에서 번호와 저장 방식을 읽습니다. 번호는 어떤 의미의 값인지 연결하고 저장 방식은 다음 값을 어떻게 읽을지 알려 줍니다.',
    '태그 정수는 필드 번호에 8을 곱한 뒤 wire type을 더합니다. 아래 세 비트를 저장 방식에 쓰므로 나머지 비트가 필드 번호가 됩니다. 예를 들어 2번 float는 wire type 5이므로 2×8+5=21입니다.',
    '21이라는 태그 정수와 스키마의 21번 필드는 전혀 다른 숫자입니다. 실제 bytes 표기는 16진수도 함께 사용하므로 10진수인지 16진수인지 표시해서 읽어야 합니다. 이 구분이 바퀴 목록의 태그를 풀 때 특히 중요합니다.'
  ],refs(27,29)),
  page('저장 방식을 구분하는 표',[
    '파서는 태그의 wire type을 보고 다음 값의 경계를 정합니다. fixed 길이는 정해진 바이트 수를 읽고, varint와 LEN은 먼저 정수의 끝 또는 길이를 확인해야 합니다.',
    '이 표는 현재 Unreal 파서가 다루는 번호입니다. 타입만으로 m/s 같은 물리 단위나 throttle이라는 의미까지 얻을 수는 없으므로 필드 번호와 스키마도 필요합니다.'
  ],refs(29),{type:'table',headers:['wire type','저장 규칙','이 프로젝트 예'],rows:[['0','계속 비트가 끝날 때까지 varint','sequence, bool, enum'],['1','8바이트 고정','Vector3d의 double'],['2','길이 varint 다음 지정 바이트','문자열, 중첩 메시지'],['5','4바이트 고정','throttle의 float']]}),
  code('태그에서 번호와 wire type을 꺼내기',P.parser,73,82,[
    'ReadTag는 태그 정수를 먼저 읽고 번호 범위를 검사합니다.',
    'Tag >> 3과 Tag & 7이 필드 번호와 아래 세 비트를 분리합니다.'
  ],refs(28)),
  page('ReadTag의 줄별 해설과 값 21',[
    '75~76행에서 태그 정수를 읽고 0인 태그를 거부합니다. 77행의 오른쪽 이동 3비트는 아래 세 비트를 제거하므로 번호를 얻습니다. 78행은 번호 0과 허용 범위를 넘는 번호를 거부하여 잘못된 필드로 진행하지 않게 합니다.',
    '79행은 검사한 번호를 출력하고 80행의 Tag & 7은 아래 세 비트만 남깁니다. 교육용 태그 정수 21은 이진수 00010101이며, 오른쪽 세 비트를 빼면 2, 아래 세 비트는 5입니다. 따라서 2번 필드의 고정 32비트 형식입니다.',
    '이 함수가 성공해도 해당 번호가 throttle인지, 읽은 값이 운전 범위 안인지는 아직 별도 문제입니다. 태그 해석은 뒤의 분기가 필요한 바이트를 읽도록 준비합니다. 여러 검사를 한 단계로 뭉뚱그리지 않는 연습입니다.'
  ],refs(28)),
  page('varint가 작은 정수를 짧게 담는 방법',[
    'varint는 정수 크기에 따라 사용하는 바이트 수가 달라지는 표현입니다. 각 바이트의 아래 7비트가 값의 조각이고, 맨 위 비트는 뒤에 조각이 더 있는지 알립니다. 작은 수는 한 바이트로 끝낼 수 있어 공간을 줄입니다.',
    '150을 128로 나누면 22와 1×128로 분해할 수 있습니다. 첫 조각 22에는 계속 표시 128을 더해 150, 즉 16진수 96을 씁니다. 다음 조각 1은 마지막이므로 계속 표시 없이 16진수 01을 씁니다.',
    '따라서 varint 150은 96 01입니다. 여기서 앞의 96은 10진수 96이 아니라 16진수 표기입니다. 읽을 때 첫 바이트의 아래 7비트 22와 다음 바이트의 1×128을 합하면 원래 값 150을 복원합니다.'
  ],refs(30)),
  code('varint를 쓰는 실제 반복문',P.parser,9,17,[
    'Value가 128 이상이면 계속 표시가 있는 바이트를 추가하고 7비트를 이동합니다.',
    '마지막 작은 조각은 계속 표시 없이 씁니다.'
  ],refs(31)),
  page('반복문에 150과 300을 넣기',[
    '11행의 조건에서 150은 128 이상이므로 반복에 들어갑니다. 13행은 아래 바이트에 0x80을 켜 0x96을 추가하고 14행은 값을 7비트 이동하여 1로 만듭니다. 이제 반복을 끝내고 16행에서 0x01을 추가합니다.',
    '300은 44+2×128입니다. 첫 바이트는 44+128=172, 즉 AC이며 마지막은 02입니다. 127은 한 바이트 7F, 128은 80 01이므로 값이 1 커질 때 바이트 수가 늘어나는 경계를 직접 볼 수 있습니다.',
    '이 방식은 여기서 다루는 unsigned 정수 사례입니다. 음수의 ZigZag 규칙까지 같은 손계산으로 추정하지 마세요. 현재 sequence와 태그의 목적을 먼저 이해하고, 다른 타입을 만났을 때 공식 형식과 스키마를 확인하는 것이 안전합니다.'
  ],refs(30,31)),
  code('읽을 범위와 정수 한도를 검사하는 코드',P.parser,57,68,[
    '반복은 입력 범위와 최대 64비트 범위를 동시에 제한합니다.',
    '63행의 마지막 바이트 검사와 68행의 계속 비트 종료 조건을 구분합니다.'
  ],refs(32)),
  page('잘린 정수와 넘치는 정수를 거부하는 이유',[
    '59행은 출력 누적값을 0으로 시작합니다. 60행은 읽을 바이트가 남아 있는지와 이동량이 64보다 작은지를 확인합니다. 62행에서 바이트를 가져오고 67행은 아래 7비트를 올바른 자리로 이동해 결과에 합칩니다.',
    '63~65행은 마지막 64비트 경계에서 허용되지 않는 상위 값이 있으면 실패합니다. 68행은 계속 비트가 0인 바이트를 만나면 읽기를 끝냅니다. 끝 표시 없이 데이터가 끝난 경우와 숫자가 범위를 넘는 경우 모두 정상 정수로 사용하면 안 됩니다.',
    '이 검사가 없다면 손상된 길이 값으로 버퍼 바깥을 읽거나 비트 넘침으로 다른 값을 만들 수 있습니다. 바이트를 받는 함수는 신뢰할 수 없는 길이와 값을 다룬다는 관점이 필요합니다. 읽기 성공 뒤에도 태그와 명령의 의미 검사가 이어집니다.'
  ],refs(32)),
  page('확인 문제: 번호와 값의 계산',[
    'A. Envelope sequence는 2번 uint64 필드입니다. 태그 정수와 16진수 태그, 값 150의 varint를 합친 바이트를 계산하세요.',
    'B. 21번 wheels가 LEN일 때 태그 정수는 얼마인가요? 그 정수를 varint로 쓰면 왜 한 바이트가 아닌가요?',
    'C. 마지막 바이트 표시가 없는 잘린 varint를 0으로 처리해 계속 진행해도 될까요? 범위 검사와 결과 유효성 관점에서 설명하세요.'
  ],refs(27,30,32),{type:'quiz'}),
  page('해설: 태그와 varint의 연결',[
    'A는 2×8+0=16이고 태그는 16진수 10입니다. 값 150은 96 01이므로 전체 조각은 10 96 01입니다. 필드 태그 뒤에 값의 varint가 이어진다는 순서를 스키마와 함께 읽어야 합니다.',
    'B는 21×8+2=170입니다. 170은 42+1×128이므로 첫 바이트 AA와 마지막 01로 표현합니다. 태그 AA 01 뒤에는 LEN의 길이 varint가 추가로 옵니다. 태그 자체도 정수이므로 varint 규칙을 사용합니다.',
    'C는 실패로 처리해야 합니다. 잘린 입력을 임의의 0으로 만들면 실제 0과 손상된 데이터를 구분할 수 없습니다. 다음 단원에서는 길이가 고정된 float와, 길이를 별도 정수로 적는 문자열·중첩 메시지를 비교하겠습니다.'
  ],refs(27,30,32),{type:'answer'}),
]));

lessons.push(lesson({id:'L08',chapter:'C4',title:'float, UTF-8과 중첩 메시지의 길이',covers:['V1-33','V1-34','V1-35','V1-36','V1-37'],
  problem:'0.5를 정수로 바꾸면 값이 사라지고, 한글 길이를 글자 수로 적으면 메시지가 잘립니다. 같은 숫자와 길이도 저장 규칙에 따라 해석이 달라집니다.',
  why:'비트 패턴 보존과 바이트 길이를 이해해야 타입 변환과 직렬화, 문자열 길이와 메시지 경계를 혼동하지 않습니다.',
  goal:'float 0.5의 네 바이트와 문자열 길이를 계산하고 Envelope 안쪽 ControlCommand를 손으로 추적합니다.',
  prerequisite:'L07의 태그·varint·wire type과 L04의 뷰 개념을 사용합니다.',next:'중첩 구조를 이해했으므로 같은 번호가 반복되는 목록과 payload 선택의 의미를 배웁니다.'},[
  page('숫자 변환과 비트 보존의 차이',[
    'float 0.5를 정수 값으로 형변환하면 소수 부분을 유지할 수 없습니다. 직렬화에서 필요한 것은 값을 정수로 반올림하는 일이 아니라 수신자가 같은 float를 복원할 수 있도록 저장된 32비트 패턴을 전달하는 일입니다.',
    'IEEE 754의 float 0.5 패턴은 16진수 3F000000입니다. Protobuf의 고정 32비트 표현은 낮은 바이트부터 쓰므로 바이트 순서는 00 00 00 3F입니다. 같은 32비트라도 어떤 규칙으로 읽는지가 의미를 결정합니다.',
    'ControlCommand의 throttle은 2번 float 필드이므로 앞에서 계산한 태그 15를 붙입니다. 결과 15 00 00 00 3F는 교육용 단일 필드 조각입니다. 지도와 세션 정보를 갖춘 전체 운전 메시지로 수락되는 패킷은 아닙니다.'
  ],refs(33)),
  code('float의 원래 비트를 꺼내는 코드',P.parser,24,33,[
    'Memcpy는 float의 비트 패턴을 Bits로 복사합니다. 숫자를 정수 값으로 변환하지 않습니다.',
    '반복문은 낮은 바이트부터 네 바이트를 차례로 씁니다.'
  ],refs(34)),
  page('WriteFixed32의 줄별 추적',[
    '26행은 uint32를 준비합니다. 27행의 static_assert는 두 객체의 크기가 같은지 컴파일할 때 확인합니다. 28행은 Value의 저장 비트를 Bits로 복사하므로 0.5의 패턴 3F000000을 그대로 보관합니다.',
    '29~31행은 Index를 0부터 3까지 바꾸며 각각 0, 8, 16, 24비트를 오른쪽 이동합니다. uint8로 바꾸어 맨 아래 바이트를 추가하므로 순서대로 00, 00, 00, 3F가 출력됩니다. 수신자는 이 네 바이트로 원래 패턴을 복원합니다.',
    '이 발췌에서 32비트 크기 확인이 모든 물리 의미까지 검증하는 것은 아닙니다. 각도인지 스로틀인지, NaN 같은 비정상 값인지에 대한 조건은 해당 메시지의 의미 검사에서 다룹니다. 표현을 보존하는 단계와 값의 허용 조건을 나누어 읽습니다.'
  ],refs(34)),
  page('LEN이 문자열과 내부 메시지를 담는 방법',[
    'LEN은 태그 뒤에 길이 varint를 쓰고, 그만큼의 바이트를 이어 붙이는 형식입니다. 길이로 문자열이나 내부 메시지의 끝을 정하면 그 뒤의 다른 필드까지 잘못 읽는 일을 막을 수 있습니다.',
    'Envelope의 source_id가 교육용 문자열 car라면 태그는 4×8+2=34, 즉 22입니다. UTF-8에서 c, a, r는 각각 63, 61, 72이므로 전체 필드 조각은 22 03 63 61 72입니다. 03은 뒤 데이터의 바이트 수입니다.',
    '한글 가는 UTF-8에서 EA B0 80의 세 바이트입니다. 글자 한 개라고 길이 1을 적으면 남은 두 바이트를 다음 필드로 오해할 수 있습니다. 실제 식별자의 허용 문자 정책은 별도이므로 이 예를 현재 source_id 허용 규칙으로 해석하지 마세요.'
  ],refs(35)),
  code('길이와 바이트를 쓰는 공통 함수',P.parser,35,40,[
    '37행은 LEN 태그를, 38행은 바이트 길이를, 39행은 원본 내용을 씁니다.',
    '이 함수는 문자열이나 이미 완성된 내부 메시지의 바이트를 담는 데 사용할 수 있습니다.'
  ],refs(36)),
  page('WriteBytes와 바깥 메시지의 역할',[
    '35행의 Bytes는 원본 바이트를 바라보는 뷰입니다. 함수는 37행에서 요청받은 필드 번호와 wire type 2를 쓰고 38행에서 Bytes.Num()을 varint로 씁니다. 39행에서 실제 바이트를 출력 배열 뒤에 붙입니다.',
    '내부 ControlCommand가 이미 5바이트라면 이 함수는 그 내용의 의미를 다시 계산하지 않습니다. 바깥 Envelope의 11번 필드에 길이 5와 내부 바이트를 담습니다. 내부 필드 번호와 바깥 필드 번호는 각 메시지 안에서 별도로 해석합니다.',
    '길이는 바이트 수를 세기 때문에 원본의 첫 바이트부터 마지막 바이트까지 정확히 포함해야 합니다. 길이 5라고 적고 4바이트만 보낸 경우 정상 중첩 메시지로 읽을 수 없습니다. 수신 측도 같은 길이 범위를 확인해야 합니다.'
  ],refs(36,37)),
  code('읽을 바이트 범위를 제한하는 코드',P.parser,102,109,[
    '길이를 먼저 읽고 현재 입력에서 남은 바이트보다 큰지 검사합니다.',
    '성공 시 내부 범위를 뷰로 돌려주고 읽기 위치를 그 길이만큼 이동합니다.'
  ],[ref(P.parser,'FReader::ReadMessage',102,109),...refs(35,36)]),
  page('중첩 조각 5A 05 15 00 00 00 3F',[
    '이 교육용 조각에서 5A는 10진수 90이며 90을 8로 나누면 필드 11과 wire type 2를 얻습니다. 다음 05는 내부 길이 5바이트입니다. 따라서 내부 파서는 15 00 00 00 3F의 다섯 바이트만 읽습니다.',
    '내부의 15는 10진수 21이므로 ControlCommand의 필드 2, wire type 5입니다. 뒤 네 바이트 00 00 00 3F는 float 0.5로 복원됩니다. Envelope의 11번과 ControlCommand의 2번은 서로 다른 메시지 층의 번호입니다.',
    'ReadMessage의 105행은 길이가 남은 입력을 넘으면 거부하고, 106~107행은 허용된 내부 범위와 다음 읽기 위치를 정합니다. 이 조각에는 Hello나 지도·세션 조건이 없으므로 바이트 계산 실습용으로만 사용합니다.'
  ],[ref(P.parser,'FReader::ReadMessage',102,109),...refs(37)]),
  page('확인 문제: 값과 길이',[
    'A. float 0.5를 정수 값 0으로 변환해 보낸 경우와 비트 패턴 3F000000을 복사해 보낸 경우의 차이를 설명하세요.',
    'B. 22 03 63 61 72에서 태그, 길이, 값 범위를 나누세요. 한글 가를 길이 1로 쓰면 왜 잘못되나요?',
    'C. 5A 05 다음에 실제로 4바이트만 남았습니다. ReadMessage의 어느 검사에서 왜 실패해야 하나요? 정상 5바이트라면 내부 float 값을 계산하세요.'
  ],refs(33,35,37),{type:'quiz'}),
  page('해설: 안쪽과 바깥쪽의 경계',[
    'A에서 정수 값 0으로 변환하면 0.5를 복원할 정보가 사라집니다. 비트 복사는 패턴을 보존하여 수신자가 float 규칙으로 같은 값을 읽게 합니다. B의 22는 4번 LEN 태그, 03은 길이, 나머지 세 바이트는 car입니다.',
    '한글 가는 UTF-8에서 세 바이트이므로 길이 1은 내용과 일치하지 않습니다. C는 길이 5가 남은 4바이트를 넘으므로 105행에서 실패합니다. 정상 조각의 내부 태그 15 뒤 네 바이트는 float 0.5입니다.',
    '중첩 메시지를 볼 때 바깥 길이를 먼저 표시하고 그 범위 안에서 새 필드 번호를 해석해 보세요. 다음 단원은 같은 내부 메시지가 여러 번 등장하는 바퀴 목록과, Envelope가 한 payload 종류를 선택하는 방식을 다룹니다.'
  ],refs(33,35,37),{type:'answer'}),
]));

lessons.push(lesson({id:'L09',chapter:'C5',title:'바퀴 목록과 payload 선택',covers:['V1-38','V1-39','V1-40','V1-41'],
  problem:'wheels=21을 바퀴 21개로 읽거나, 목록 두 번째 원소를 무조건 앞오른쪽 바퀴로 표시하면 정상 데이터를 잘못 그릴 수 있습니다.',
  why:'목록의 필드 번호, 원소의 위치 식별값, 메시지 종류 선택을 구분해야 수신 순서나 숫자의 모양에 의존하지 않습니다.',
  goal:'repeated WheelState와 wheel_index를 실제 수신 코드에 대응하고 oneof의 역할을 설명합니다.',
  prerequisite:'L07~L08의 태그·LEN·중첩 메시지 경계를 사용합니다.',next:'여러 값의 구조를 알았으므로 다음에는 값이 없는 상태와 기본값, 호환성 문제를 다룹니다.'},[
  page('목록이 필요한 실제 상황',[
    '차량에는 여러 바퀴의 접지 여부와 회전값이 필요합니다. 왼쪽과 오른쪽의 값이 다를 수 있으므로 차량 값 하나만으로 모든 바퀴 상태를 설명할 수 없습니다. 같은 형태의 WheelState를 여러 개 담는 목록이 필요합니다.',
    'repeated는 같은 필드에 원소를 여러 개 담을 수 있다는 선언입니다. EntityState의 wheels=21에서 21은 그 목록을 구분하는 필드 번호입니다. 원소 수는 실제 보낸 목록에 따라 달라지며 숫자 21이 바퀴 개수를 정하지 않습니다.',
    '각 원소는 별도 wheel_index로 위치를 알려 줍니다. 목록의 첫 번째라는 순서와 앞왼쪽이라는 의미를 무조건 같게 취급하면, 정상 식별값을 가진 다른 순서의 데이터도 잘못 표시할 수 있습니다. 위치 계약을 별도로 읽어야 합니다.'
  ],refs(38,39)),
  code('바퀴 원소의 실제 선언',P.proto,52,57,[
    'wheel_index는 WheelState 안의 1번 필드이며 그 값이 바퀴 위치를 나타냅니다.',
    '접지와 조향각, 회전 속도도 같은 원소 안에 들어갑니다.'
  ],[ref(P.proto,'WheelState',52,57),...refs(39)]),
  page('바퀴 선언과 위치 번호의 의미',[
    '52행은 원소 하나의 타입을 시작합니다. 53행의 오른쪽 1은 wheel_index 필드 번호이고 실제 값 0, 1, 2, 3이 각각 앞왼쪽, 앞오른쪽, 뒤왼쪽, 뒤오른쪽을 뜻합니다. 같은 줄 안의 숫자도 역할이 다릅니다.',
    '54행은 접지 여부를, 55행은 해당 바퀴의 조향각을 담습니다. 56행의 angular_speed는 rad/s 단위이며 전진 방향 부호의 회전 속도입니다. 실제 화면 표시도 이 계약에 맞는 바퀴와 축을 찾아 값을 적용해야 합니다.',
    '교육용으로 목록 두 번째 원소의 wheel_index가 3이라면 그 원소는 뒤오른쪽 바퀴입니다. 목록 인덱스가 1이므로 앞오른쪽이라고 바꾸어 읽지 않습니다. 여기서는 바이트를 읽은 뒤 의미를 어디에 적용할지가 핵심입니다.'
  ],refs(39)),
  code('21번 필드를 만날 때 원소를 추가',P.parser,542,549,[
    '각 LEN 필드를 WheelState 하나로 파싱하여 목록에 추가합니다.',
    '32개 제한은 과도한 입력을 막는 수신 한도이며 정상 바퀴 수의 선언이 아닙니다.'
  ],refs(40)),
  page('목록 수신 코드의 줄별 의미',[
    '542행은 EntityState의 21번 필드를 처리합니다. 543행은 누적 원소가 이미 32개면 실패하여 비정상적으로 큰 목록을 제한합니다. 544행은 LEN 형식인지 확인하고 545행은 해당 원소의 내부 바이트 범위를 읽습니다.',
    '546행은 임시 Wheel 객체를 만들고 547행에서 내부 필드를 파싱합니다. 실패하면 목록에 넣지 않습니다. 548행은 성공한 원소만 추가하므로, 다음 21번 필드가 오면 같은 과정을 반복하여 또 다른 바퀴 상태를 쌓습니다.',
    '메시지 원소가 반복되는 이 구조를 숫자 배열의 packed 표현과 섞어 이해하지 마세요. 각 바퀴는 자체 필드와 길이를 가진 내부 메시지입니다. 바깥 반복 횟수와 내부 wheel_index 값은 각각 별도로 검사하고 사용해야 합니다.'
  ],refs(38,40)),
  code('Envelope가 선택하는 payload 종류',P.proto,284,290,[
    'oneof는 여러 종류 중 어떤 payload가 활성인지 표현합니다. 아무 종류도 선택되지 않은 상태도 가능합니다.',
    'WorldState 안의 여러 엔티티 목록과 Envelope의 payload 선택은 다른 층입니다.'
  ],refs(41)),
  page('oneof가 서로 다른 요청을 구분하는 이유',[
    'Envelope가 Hello인지 운전 명령인지 구분해야 합니다. 여러 필드를 동시에 활성 요청으로 취급하면 행동이 모호해집니다. oneof는 생성 메시지에서 한 payload 종류의 활성 상태를 표현합니다.',
    '284행의 payload 아래 10~14번은 각각 다른 종류의 내부 메시지입니다. Hello를 설정했다가 ControlCommand를 설정하면 활성 종류가 바뀝니다. 여러 알려진 종류가 반복된 바이트는 마지막 종류를 선택하므로 첫 종류만 쓴다고 가정하지 않습니다.',
    'WorldState 안에는 여러 차량과 바퀴가 들어갑니다. 내부 repeated의 개수는 바깥 oneof 선택과 충돌하지 않습니다. 알려지지 않은 미래 종류는 구 수신기의 해석 한계도 따로 검토해야 합니다.'
  ],refs(41)),
  page('확인 문제: 세 종류의 번호',[
    'A. repeated WheelState wheels=21, wheel_index=1이라는 선언, 실제 wheel_index 값 3을 각각 한 문장으로 풀어 적으세요.',
    'B. 수신 코드의 32개 제한을 보고 정상 차량이 바퀴 32개를 보낸다고 결론 내릴 수 있나요? 543행과 548행의 목적을 구분하세요.',
    'C. Envelope가 WorldState payload를 선택하면서 그 안에 NPC 8개를 담는 것은 oneof와 모순되나요? 종류 선택과 원소 개수를 나누어 설명하세요.'
  ],refs(38,39,40,41),{type:'quiz'}),
  page('해설: 필드, 위치값, 종류 선택',[
    'A의 21은 EntityState 안에서 바퀴 목록을 식별하고, 선언의 1은 WheelState 안에서 wheel_index 필드를 식별합니다. 실제 값 3은 뒤오른쪽 위치를 뜻합니다. 메시지의 층과 숫자의 역할을 함께 적으면 혼동을 줄일 수 있습니다.',
    'B의 32는 파서가 과도한 입력을 거부하는 한도입니다. 548행은 성공한 원소를 하나씩 추가합니다. C는 모순이 아닙니다. 바깥 Envelope의 활성 종류는 WorldState 하나이고 그 내부 목록에는 여러 엔티티를 담을 수 있습니다.',
    '이제 데이터의 위치와 개수를 읽을 수 있어야 합니다. 다음 단원은 필드가 아예 오지 않은 경우를 살펴봅니다. 특히 기어 값 0과 기어 정보를 보내지 않은 상태가 왜 서로 다른 운전 결과가 되는지 실제 코드로 확인합니다.'
  ],refs(38,39,40,41),{type:'answer'}),
]));

lessons.push(lesson({id:'L10',chapter:'C5',title:'값의 부재, 기본값과 스키마 호환성',covers:['V1-42','V1-43','V1-44','V1-45','V1-46','V1-47'],
  problem:'구 클라이언트가 gear를 보내지 않는 경우와 운전자가 Neutral을 선택한 경우를 같은 0으로 처리하면 기존 동작이 달라집니다.',
  why:'부재와 명시적 값의 차이를 보존하고 구 수신기의 한계를 이해해야 필드 추가가 기존 운전을 조용히 바꾸지 않습니다.',
  goal:'optional gear 분기와 기본값을 추적하고 unknown 필드 처리·파싱 성공·의미 검증을 구분합니다.',
  prerequisite:'L09의 필드와 oneof, L03의 optional 개념을 사용합니다.',next:'유효한 값이라도 현재 지도와 조종 연결에 속해야 하므로 Hello와 세션 검사를 배웁니다.'},[
  page('기어를 보내지 않은 클라이언트',[
    '구 클라이언트가 gear를 보내지 않는다고 가정합니다. 부재를 Neutral로 처리하면 기본 Drive로 움직이던 클라이언트가 업데이트 뒤 움직이지 않을 수 있습니다. 부재 정책도 동작 계약입니다.',
    '반대로 운전자가 명시적으로 Neutral을 선택했다면 그 요청은 실제 중립으로 반영해야 합니다. gear의 enum 값 0이 Neutral이므로 기본 숫자 0만 보면 정보가 없었는지 사용자가 중립을 보냈는지 구분하기 어렵습니다.',
    'Protobuf optional은 필드가 존재했는지와 저장된 값을 구분하게 합니다. 현재 decoder는 존재할 때만 기어를 변환하며, 없으면 내부 VehicleInput의 기본 Drive를 유지합니다. 이 정책은 문법에서 자동으로 정한 것이 아니라 프로젝트가 선택한 동작입니다.'
  ],refs(42,43)),
  code('존재 여부를 보존하는 gear 선언',P.proto,237,242,[
    '240행의 optional은 gear 값과 필드 존재 여부를 구분하게 합니다.',
    '일반 숫자 필드와 달리 has_gear 분기를 통해 부재를 확인할 수 있습니다.'
  ],refs(42)),
  code('gear가 있을 때만 바꾸는 실제 분기',P.decoder,75,83,[
    '스로틀·브레이크·조향을 읽은 뒤, has_gear가 참일 때만 기어를 대입합니다.',
    '이 분기가 기본 Drive와 명시적 Neutral의 차이를 유지합니다.'
  ],refs(43)),
  page('세 가지 입력을 넣은 기어 추적',[
    '75~78행은 운전 값들을 내부 입력으로 옮깁니다. 79행의 has_gear가 거짓이면 80행을 실행하지 않으므로 기본 Drive가 남습니다. 앞의 VehicleInput 초기값을 함께 읽어야 이 분기의 실제 결과를 알 수 있습니다.',
    'gear를 명시적으로 0, 즉 Neutral로 보냈다면 has_gear는 참이고 80행에서 중립으로 변환합니다. Reverse를 보낸 경우도 같은 분기로 해당 값을 변환합니다. 값 0이라는 이유만으로 필드가 없다고 판단하면 명시적 중립 요청을 잃습니다.',
    '83행은 파싱한 메시지 결과를 반환합니다. 아직 현재 연결이 운전할 권한을 얻었다는 뜻은 아닙니다. 이 추적은 decoder의 기어 해석 정책을 설명하며, 이후 호스트의 지도·세션·시간 검사가 추가로 입력을 거부할 수 있습니다.'
  ],refs(43)),
  page('일반 기본값을 최신 정보로 오해하는 경우',[
    'proto3의 일반 숫자 필드는 바이트에서 생략되면 getter가 기본값 0을 돌려줍니다. 이때 0이라는 숫자만으로 정보가 실제로 제공됐는지 알 수 없는 경우가 있습니다. 값이 의미를 갖는 조건을 다른 필드나 실행 맥락에서 확인해야 합니다.',
    '현재 Health의 입력 나이가 0이라고 해서 방금 정상 명령을 받았다고 단정할 수 없습니다. has_control_command가 정상 입력 수락 이력이 있는지를 별도로 알려 줍니다. 초기 상태와 실제 최신 입력 상태를 구분하기 위한 정보입니다.',
    '따라서 화면에 숫자를 그릴 때도 단위뿐 아니라 유효성 조건을 확인합니다. 없는 지면을 높이 0으로 만든 L03의 오류와 같은 원리입니다. 기본값을 읽을 수 있다는 사실이 그 값의 실제 관측이나 수락 이력을 증명하지는 않습니다.'
  ],refs(44)),
  page('필드 추가가 기존 프로그램에 미치는 영향',[
    '새 번호로 필드를 추가하면 구 수신기는 그 필드의 의미를 모를 수 있습니다. 구조를 건너뛰고 기존 값으로 계속 동작할 수 있는지, 새 값이 없을 때도 안전한 기본 동작이 있는지를 먼저 확인해야 호환성을 설명할 수 있습니다.',
    '기존 번호를 다른 뜻에 재사용하면 과거 기록을 새 의미로 잘못 읽을 수 있습니다. 제거한 번호를 다시 쓰지 않도록 reserved를 검토하는 이유입니다. 이름만 바꾸거나 타입이 같다는 사실로 바이트와 의미의 호환성을 보장할 수 없습니다.',
    '단위를 바꾸는 변경은 파싱이 성공해도 의미를 깨뜨릴 수 있습니다. 새 기능이 반드시 필요하다면 Hello의 기능 협상 정책도 바뀔 수 있습니다. 호환성은 파서가 죽지 않는지뿐 아니라 어떤 동작을 계속 보장하는지까지 포함합니다.'
  ],refs(45)),
  code('알 수 없는 필드를 넘기는 실제 분기',P.parser,130,138,[
    '필드 의미를 몰라도 wire type으로 길이를 읽어 다음 필드 위치로 갈 수 있습니다.',
    '현재 자체 파서는 0, 1, 2, 5만 지원하며 다른 wire type은 실패합니다.'
  ],refs(46)),
  page('건너뛰기 코드와 호환성의 한계',[
    '132행은 varint 하나를 읽어 버립니다. 133행은 남은 데이터가 충분할 때 8바이트를 넘깁니다. 134행은 길이를 읽어 내부 메시지 범위를 건너뛰고 135행은 같은 방식으로 4바이트를 넘깁니다. 모두 입력 범위 확인이 필요합니다.',
    '이 코드는 알 수 없는 필드의 기능을 구현하지 않습니다. 예를 들어 새 상태 플래그를 건너뛰면 구 클라이언트는 그 플래그를 표시하거나 판단에 사용할 수 없습니다. 구조적 통과와 기능 지원을 구분해야 새 기능의 지원 범위를 정확히 말할 수 있습니다.',
    '136행에서 지원하지 않는 wire type은 실패합니다. Unreal 파서를 모든 Protobuf 형식의 범용 구현이라고 부르면 실제 한계를 숨깁니다. 스키마를 확장할 때 해당 형식과 수신 분기를 함께 확인해야 하는 이유입니다.'
  ],refs(46)),
  code('형식 파싱 실패를 반환하는 코드',P.decoder,39,43,[
    'ParseFromArray가 Envelope를 복원하지 못하면 오류를 남기고 nullopt로 끝냅니다.',
    '파싱 성공 뒤에는 schema_version과 명령 의미 조건을 이어서 검사합니다.'
  ],refs(47)),
  page('파싱 성공이 입력 수락과 다른 이유',[
    '39행은 생성 Envelope 객체를 준비하고 40행은 받은 바이트를 해석합니다. 실패하면 41행이 오류 문장을 채우고 42행은 결과 부재를 반환합니다. 이 경로의 실패는 우선 바이트에서 계약된 형태를 복원하지 못했다는 뜻입니다.',
    '형태 복원에 성공해도 다음 검사에서 현재 schema_version과 다르면 거부할 수 있습니다. 현재 값은 왼쪽 양수 FLU 스칼라 계약을 나타내는 버전 2입니다. 같은 숫자를 다른 방향 계약으로 해석하는 일을 막기 위해 버전을 확인합니다.',
    '그 뒤 호스트는 지도와 조종 연결, 시간까지 검사합니다. 오래된 명령도 문법적으로 완전한 메시지일 수 있으므로 파서 성공을 안전한 입력의 증거로 쓰면 안 됩니다. 이 구분이 다음 연결 생명주기 단원의 출발점입니다.'
  ],refs(47)),
  page('확인 문제: 0과 부재, 확장과 수락',[
    'A. gear 부재와 명시적인 gear=Neutral은 현재 decoder에서 각각 어떤 내부 값이 되나요? has_gear와 초기값을 근거로 설명하세요.',
    'B. Health의 age가 0이면 반드시 방금 입력을 받은 상태인가요? 추가로 볼 필드를 적으세요. 새 필드를 건너뛰었다는 사실은 새 기능 지원을 뜻하나요?',
    'C. 이미 쓰인 필드 번호에 새 의미를 넣으면 어떤 기록 문제가 생기나요? ParseFromArray가 성공했는데도 버전이나 세션 때문에 거부할 수 있는 이유도 적으세요.'
  ],refs(42,43,44,45,46,47),{type:'quiz'}),
  page('해설: 형식만 맞아서는 충분하지 않습니다',[
    'A에서 부재는 기본 Drive를 유지하고 명시적 Neutral은 Neutral로 변환합니다. B는 has_control_command를 함께 봐야 합니다. 필드 건너뛰기는 구조적 진행일 뿐 새 의미를 화면이나 계산에 적용했다는 증거가 아닙니다.',
    'C에서 번호 재사용은 과거 바이트를 새로운 뜻으로 오해하게 만듭니다. 파싱은 값을 복원하는 단계이고 버전·세션 검사는 그 값을 현재 계약과 제어 흐름에서 받아도 되는지 판단하는 단계이므로 뒤에서 실패할 수 있습니다.',
    '이제 스키마 변경을 설명할 때 생성 성공이나 파싱 성공만 말하지 말고 기본 동작과 구 수신기 처리, 의미 검증을 함께 적어 보세요. 다음 단원은 현재 세계와 운전 주체를 구분하는 식별자를 실제 재연결 사례로 배웁니다.'
  ],refs(43,44,45,46,47),{type:'answer'}),
]));

lessons.push(lesson({id:'L11',chapter:'C6',title:'Hello, 지도, 연결과 Play의 구분',covers:['V1-48','V1-49','V1-50','V1-51','V1-52'],
  problem:'다른 지도 자료를 쓰는 클라이언트나 이미 끝난 Play의 늦은 메시지가 현재 차량을 조종하거나 초기화하면 안 됩니다.',
  why:'연결이 열렸다는 사실만으로 같은 기능·세계·실행이라고 가정하지 않도록 각 식별 정보의 목적을 나누어 배웁니다.',
  goal:'Hello의 필수 기능, 지도 checksum, source/session/play 식별, 순번과 거부 후 상태 보존을 설명합니다.',
  prerequisite:'L06의 Envelope와 L10의 형식/의미 검증 차이를 사용합니다.',next:'같은 주체의 순서 있는 명령도 너무 늦으면 위험하므로 마지막 수락 시각과 ControlLease를 배웁니다.'},[
  page('정상 바이트가 잘못된 세계에 도착한 경우',[
    'Unreal은 언덕을 측정했는데 서버가 예전 평지 자료를 쓰고 있다고 가정합니다. 좌표 숫자가 같아도 지면과 충돌 해석이 다르므로 두 프로그램은 같은 세계에서 계산한다고 볼 수 없습니다. 지도 식별 검사 없이 운전 명령을 받으면 오류가 커집니다.',
    '또한 이전 Play에서 만들어 둔 초기화 메시지가 늦게 도착할 수 있습니다. 새 Play가 이미 시작됐는데 이를 받아들이면 현재 차량이 출발점으로 돌아가거나 시뮬레이션 시간이 바뀔 수 있습니다. 메시지 내용 외에 어느 실행의 요청인지가 필요합니다.',
    '현재 호스트는 Hello의 기능 조건과 지도 계약, 연결과 Play 식별을 확인합니다. 각 검사는 다른 위험을 막으므로 이름이 비슷하다고 하나의 문자열로 합칠 수 없습니다. 먼저 기능을 맞추는 Hello부터 보겠습니다.'
  ],refs(48,49,50,51)),
  code('현재 Hello의 필수 기능 목록',P.session,248,257,[
    '현재 생산 경로는 네 가지 capability가 모두 있는지 검사합니다.',
    'WebSocket 연결 성공 뒤에도 이 애플리케이션 Hello 검사가 필요합니다.'
  ],refs(48)),
  page('Hello 검사와 지도 checksum의 목적',[
    '248~253행은 WorldState v2, Control v2, reset, 지도 checksum의 필수 기능을 선언합니다. 254행부터 순회하고 255행에서 없으면 256~257행이 이유를 남기고 거부합니다. Hello는 기능 계약을 확인하는 절차입니다.',
    '지도 이름은 사람이 자료를 찾는 이름이고 checksum은 자료 일치 확인의 단서입니다. 같은 레벨 이름으로 다시 bake해도 내용이 바뀌면 설정한 checksum과 맞아야 합니다. 현재 호스트는 맞지 않는 지도의 입력을 거부합니다.',
    'checksum 일치가 사용자 인증이나 해시 충돌 가능성 제거를 뜻하지는 않습니다. 여기서는 계산 자료를 맞추려는 계약입니다. 기능 협상, 자료 식별, 사용자를 인증하는 보안 문제를 구분해야 합니다.'
  ],refs(48,49)),
  page('식별자가 나누는 수명',[
    'source_id는 누가 보낸 흐름인지 나타내고 session_id는 어느 연결 수명인지 구분합니다. 같은 클라이언트가 재연결하면 출처는 같아도 새 연결은 새 session_id를 사용합니다.',
    'play_session_id는 어느 Play 실행인지를 구분합니다. 한 Play 안에서 연결을 다시 맺을 수 있으므로 연결과 Play의 수명이 다릅니다. 서버의 connection_generation은 이전 소켓 콜백을 구분하는 로컬 세대 정보입니다.'
  ],refs(50),{type:'table',headers:['정보','구분하는 대상','현재 재연결 예'],rows:[['source_id','명령을 보내는 출처','같은 클라이언트 출처 유지'],['session_id','연결 수명','새 연결이면 새 값'],['play_session_id','Play 실행','같은 Play면 유지'],['connection_generation','서버가 발급한 소켓 세대','이전 소켓의 늦은 콜백 차단']]}),
  page('Play와 연결의 서로 다른 생명주기',[
    '위쪽 도로는 Play 실행, 아래 케이블은 통신 연결의 수명을 비유합니다. 한 Play에서 연결을 다시 맺어도 차량 상태를 보존할 수 있습니다. 따라서 연결이 바뀌었다는 사실만으로 새 Play의 초기화를 실행하면 안 됩니다.',
    '이 그림은 수명 구분용 개념도이며 케이블의 가로 정렬을 실제 소켓 재사용 절차로 읽지 않습니다. 현재 새 Play는 새 연결 절차를 거치며, 동일 소켓 세대에서 play_session_id를 바꾸는 요청은 서버가 거부합니다.'
  ],[ref(P.session,'SimulationHost::handle_simulation_reset',455,475),...refs(50,51)],{type:'picture',imageKey:'session'}),
  page('같은 Play 재연결과 새 Play',[
    '교육용 출처 U, Play P1, 연결 A가 운전 중입니다. 연결이 끊겨 B로 다시 연결되어도 Play가 P1이면 차량을 출발점으로 되돌릴 이유가 없습니다. 정상 재연결은 물리 상태를 보존하고 새 제어 연결을 묶습니다.',
    '반면 사용자가 Stop 뒤 새 Play P2를 시작하면 초기화해야 할 실행이 달라집니다. 현재 정상 새 Play reset은 플레이어와 교통 상태, 구조물, 계산 시간을 초기화합니다. 같은 단어 reset이 등장해도 어떤 실행에 대한 요청인지 검사해야 합니다.',
    '끝난 P1의 지연 reset은 P2를 덮어쓰면 안 됩니다. 서버는 옛 Play 이력과 소켓 세대를 확인합니다. 동일 연결 세대에서 신원을 바꾸거나 같은 Play에서 차종을 바꾸는 요청도 정상 재연결로 취급하지 않습니다.'
  ],refs(50,51)),
  code('같은 Play의 재연결에서 먼저 하는 일',P.session,509,518,[
    '재연결 후보가 이미 폐기된 세션이면 거부하고, 허용되면 우선 안전 입력을 적용합니다.',
    '이 경로는 위치 초기화를 호출하지 않으며 새 정상 입력을 다시 기다립니다.'
  ],[ref(P.session,'SimulationHost::handle_simulation_reset',509,518),...refs(51)]),
  page('재연결 코드의 줄별 의미',[
    '509~510행은 새 연결 식별을 ControlLease에 전달합니다. 511~515행은 이미 폐기한 세션이면 다시 제어하도록 허용하지 않습니다. 오래된 연결의 패킷이 나중에 도착해도 현재 운전을 되살리지 않게 하는 경계입니다.',
    '516행에서 준비된 재연결이면 517행이 안전 정지 입력을 만들고 518행이 적용합니다. 이후 현재 연결의 식별을 갱신하지만 플레이어 위치를 초기화하는 호출은 이 분기에 없습니다. 정상 명령이 다시 수락되기 전까지 이전 스로틀을 계속 쓰지 않도록 합니다.',
    '같은 Play라는 정보만으로 권한을 얻지 않습니다. 앞뒤의 지도·신원·세대 검사를 통과한 재연결만 이 경로를 사용합니다. E-stop이 이미 걸린 상태는 별도 정책이므로 일반 재연결 설명으로 자동 해제된다고 추정하면 안 됩니다.'
  ],[ref(P.session,'SimulationHost::handle_simulation_reset',509,535),...refs(51)]),
  code('새 Play가 초기화하는 실제 상태',P.session,551,562,[
    '새 Play 검증을 통과한 분기에서 차량과 런타임 상태를 초기화합니다.',
    '마지막에 안전 입력을 적용하고 시뮬레이션 경과 시간을 다시 시작합니다.'
  ],[ref(P.session,'SimulationHost::handle_simulation_reset',551,562),...refs(51)]),
  page('순번이 필요한 이유와 거부 후 상태 보존',[
    '같은 제어 흐름에서 sequence는 이미 처리한 입력보다 새로운지 판단하는 번호입니다. 101을 수락한 뒤 100이 오면 TCP 연결이 살아 있어도 최신 입력으로 채택하면 안 됩니다. 재접속과 재전송 맥락은 단순 전달 순서만으로 해결되지 않습니다.',
    '현재 비교는 마지막 정상 순번 이하를 거부합니다. 중요한 점은 거부한 높은 번호가 마지막 정상 번호를 갱신하면 안 된다는 것입니다. 예를 들어 잘못된 시간의 1000을 거부한 뒤 정상 102가 영구히 오래된 것으로 막히면 검사가 상태를 오염시킨 것입니다.',
    '그래서 검증을 모두 통과한 뒤 순번과 마지막 수신 시각을 저장합니다. 바이트가 왔다는 기록과 운전 명령을 수락했다는 기록을 구분하는 이유입니다. 다음 단원의 시간 검사도 이 마지막 정상 입력을 기준으로 계산합니다.'
  ],refs(52)),
  code('오래된 순번을 즉시 거부하는 비교',P.lease,109,116,[
    '같은 연결의 마지막 정상 상태를 기준으로 이번 sequence를 비교합니다.',
    '이 비교에서 실패하면 뒤의 수락 상태 저장까지 진행하지 않습니다.'
  ],[ref(P.lease,'ControlLease::accept',109,116),...refs(52)]),
  page('확인 문제: 세션과 과거 메시지',[
    'A. U/P1/A가 끊긴 뒤 U/P1/B로 정상 재연결했습니다. 차량 위치 초기화 여부와 다시 필요한 입력 조건을 설명하세요.',
    'B. 현재 P2를 플레이 중인데 이미 끝난 P1의 reset이 도착했습니다. 지도 checksum과 순번이 커 보이면 받아도 되나요?',
    'C. 마지막 정상 순번 101에서 시간 검사를 실패한 1000이 왔고 다음에 정상 102가 왔습니다. 순번 저장을 어느 시점에 해야 102를 올바르게 평가할 수 있나요?'
  ],refs(49,50,51,52),{type:'quiz'}),
  page('해설: 현재 실행의 권한만 유지',[
    'A는 같은 Play의 정상 재연결이므로 물리 위치를 보존합니다. 제어 연결을 B로 바꾸고 안전 입력 상태에서 새 정상 명령을 기다립니다. B는 옛 Play의 지연 reset이므로 현재 상태를 초기화하도록 수락하면 안 됩니다.',
    'C는 모든 검증을 통과한 뒤에만 순번을 저장해야 합니다. 거부된 1000은 정상 기준 101을 바꾸지 않아야 다음 102를 새 입력으로 평가할 수 있습니다. 높은 순번만으로 지도나 연결, 시간 조건을 우회할 수는 없습니다.',
    '현재라는 말은 시간만 뜻하지 않습니다. 기능과 지도, 출처와 연결, Play, 순번이 모두 맞아야 합니다. 다음 단원은 이 조건이 맞는 명령도 지연되거나 끊기면 왜 거부하거나 안전 정지해야 하는지 시간선으로 설명합니다.'
  ],refs(48,49,50,51,52),{type:'answer'}),
]));

lessons.push(lesson({id:'L12',chapter:'C6',title:'ControlLease가 오래된 운전을 막는 방법',covers:['V1-53','V1-54','V1-55'],
  problem:'스로틀 1을 마지막으로 받은 뒤 조종 연결이 끊겼습니다. 서버가 같은 입력을 계속 계산하면 운전자가 손을 뗐는데도 차가 가속할 수 있습니다.',
  why:'현재 입력을 보내는 주체에게만 제한된 시간 동안 제어를 맡기고, 지연된 명령이나 무입력 상태를 별도 기준으로 처리해야 합니다.',
  goal:'서버 관측 시각과 클라이언트 생성 시각을 구분하고, 100ms 지연 추정·250ms SafeStop·1000ms 연결 폐기를 코드와 숫자로 판정합니다.',
  prerequisite:'L11의 현재 연결·순번·수락 상태 저장과 L02의 이벤트 루프 개념을 사용합니다.',
  next:'입력 수락 조건을 모두 배웠으므로 마지막 단원에서 송신부터 화면 반영까지 한 명령을 전체 경로로 추적합니다.'},[
  page('마지막 스로틀을 언제까지 믿을 수 있을까요?',[
    '서버가 스로틀 1을 받은 다음 조종 프로그램이 멈췄다고 가정합니다. 입력 갱신이 없다는 이유로 기존 스로틀을 계속 유지하면 운전자는 이미 제어할 수 없는데 차는 가속할 수 있습니다. 입력이 끊겼을 때의 안전 정책이 필요합니다.',
    'ControlLease는 현재 제어 주체와 마지막 정상 입력을 관리하는 시간 제한 제어권입니다. 정상 연결과 순서를 가진 명령만 수락하고, 한동안 정상 입력이 없으면 안전 입력으로 바꿉니다. 오래 끊기면 이전 연결을 폐기하여 다시 연결하도록 합니다.',
    '동시에 너무 늦게 도착한 명령도 그대로 실행하면 안 됩니다. 이미 손을 뗀 과거 스로틀이 뒤늦게 적용되면 반응이 쌓여 보일 수 있기 때문입니다. 그래서 입력의 지연 추정과 정상 입력이 없는 기간을 서로 다른 기준으로 확인합니다.'
  ],refs(54,55)),
  page('시간 숫자 세 종류의 목적',[
    '단조 시계는 뒤로 가지 않으며 경과 시간을 재는 데 씁니다. client_time_ns는 클라이언트의 입력 생성 시각, received_at은 서버의 메시지 처리 입구 관측 시각입니다. 시작 기준이 다른 두 시계의 절대값 차이는 정확한 통신 지연이 아닙니다.',
    'now는 검사를 실행하는 현재 서버 시각입니다. 여기서 마지막 정상 received_at을 빼면 정상 입력이 없던 기간입니다. 같은 서버 시계의 차이이므로 클라이언트 시계와 맞출 필요가 없습니다.',
    'ns는 10억분의 1초이며 1ms는 100만ns입니다. simulation_time_ns는 고정 틱의 누적 계산 시간, EntityState.timestamp는 Unix 초입니다. 이 값들을 제어권의 단조 경과 시간과 섞으면 잘못 계산합니다.'
  ],refs(53)),
  page('입력 시각과 무입력 기간의 구분',[
    '그림의 Client stamp와 Server arrival은 각각 입력 생성과 서버 수신 관측 시점을 뜻합니다. 서로 다른 시계의 숫자를 직접 빼지 않고, 각 시계 안에서 이전 정상 입력과의 간격을 먼저 구합니다.',
    'Silence duration과 Safe stop, Disconnect는 정상 입력 후 서버 경과 시간을 설명합니다. 세 임계값을 같은 축에 놓은 수치 그래프가 아닙니다. 100·250·1000ms 조건은 다음 코드와 표에서 따로 계산합니다.'
  ],refs(53,54,55),{type:'picture',imageKey:'lease_time'}),
  page('간격 차이로 지연을 추정하는 이유',[
    '교육용으로 이전 정상 입력의 클라이언트 시각을 1000ms, 서버 수신 시각을 5000ms라고 두겠습니다. 다음 입력의 클라이언트 시각이 1020ms이면 생성 간격은 20ms입니다. 서버가 그 입력을 5180ms에 처리하면 수신 간격은 180ms입니다.',
    '시계 시작점 차이 4000ms를 지연으로 쓰지 않습니다. 각 시계 안의 차이를 구한 뒤 180−20=160ms를 계산합니다. 입력이 만들어진 간격보다 서버가 관측한 간격이 더 늘어난 양을 대기시간 추정으로 사용하는 것입니다.',
    '현재 최대 추정 대기는 100ms이므로 이 입력은 거부됩니다. 이 수치는 모든 네트워크 지연을 정확히 측정하는 장치가 아닙니다. 이전 정상 입력 이후 상대적인 지연 증가를 보는 보호 조건이며 첫 입력에는 이전 비교 기준도 아직 없습니다.'
  ],refs(55)),
  code('대기시간 추정과 거부 조건',P.lease,123,132,[
    '서버 수신 간격에서 클라이언트 생성 간격을 뺀 양수 부분을 사용합니다.',
    '추정값이 한도를 초과하면 ExcessiveQueueAge로 거부합니다.'
  ],refs(55)),
  page('지연 검사 코드의 줄별 해설',[
    '123행은 이번 received_at에서 이전 정상 수신 시각을 뺍니다. 124~125행은 두 client_time_ns의 차이를 나노초 기간으로 만듭니다. 두 간격을 비교하므로 서로 다른 컴퓨터의 절대 시계 시작점은 직접 빼지 않습니다.',
    '126~129행은 서버 간격이 더 클 때만 차이를 쓰고 그 외에는 0으로 제한합니다. 130행의 >는 한도와 같으면 이 조건만으로 거부하지 않고 초과할 때 거부한다는 뜻입니다. 131행에서 반환하면 정상 수락 기록을 갱신하지 않습니다.',
    '서버 간격 80ms, 생성 간격 100ms면 추정값은 0입니다. 서버 간격 120ms, 생성 간격 20ms면 정확히 100ms이고, 121ms라면 101ms로 이 검사를 실패합니다. 다른 세션·순번 조건은 정상이라는 가정입니다.'
  ],refs(55)),
  page('거부한 패킷이 정상 입력을 연장하지 않습니다',[
    '이전 정상 수신이 5000ms인데 5180ms의 명령이 지연으로 거부됐다면 마지막 정상 수신 시각은 5000ms에 남습니다. 다음 시간 검사에서 5180을 기준으로 수명을 연장하면 잘못된 명령도 계속 제어권을 살리는 결과가 됩니다.',
    '같은 원리로 거부한 높은 sequence도 마지막 정상 순번을 바꾸지 않습니다. 수신된 패킷 수와 정상적으로 수락한 입력 수가 다른 이유입니다. 입력이 계속 들어와도 모두 지연·세션 검사에서 실패하면 무입력 안전 정지에 도달할 수 있습니다.',
    '따라서 로그에서 통신이 계속 있다는 사실만으로 제어권이 정상이라고 판단하지 않습니다. 수락 여부와 거부 이유, 마지막 정상 입력의 나이를 함께 봅니다. 이제 그 정상 입력이 끊긴 기간을 검사하는 soft timeout 코드로 이어집니다.'
  ],refs(52,54,55)),
  code('정상 입력 공백을 검사하는 soft timeout',P.lease,155,165,[
    '현재 서버 시각 now와 마지막 정상 수신 시각의 차이를 timeout_과 비교합니다.',
    'SafeStop은 먼저 제어를 안전한 상태로 바꾸되 현재 소유권을 유지합니다.'
  ],[ref(P.lease,'ControlLease::update_timeout',155,165),...refs(54)]),
  page('250ms 안전 정지는 어떤 일을 하나요?',[
    '157~159행은 이미 SafeStop이거나 정상 입력을 아직 받지 않았다면 새 전환을 만들지 않습니다. 160~162행은 공백이 한도 이하이면 그대로 둡니다. 초과하면 163~164행이 안전 정지 전환을 알려 줍니다.',
    '호스트는 이 결과를 받아 안전 입력을 적용합니다. 현재 make_safe_stop_input은 기본 스로틀 0에 브레이크 1과 handbrake를 설정합니다. 차 위치를 순간 고정하는 대신 물리 계산이 이 입력으로 감속하도록 하므로 속도가 한순간에 0이 된다는 뜻은 아닙니다.',
    '잠깐의 지연마다 끊으면 재접속 비용이 늘므로 soft timeout은 소유권을 유지합니다. 같은 소유자의 새롭고 순서가 맞는 입력이 다시 모든 검사를 통과하면 연결을 끊지 않고 조작을 회복할 수 있습니다.'
  ],[ref(P.lease,'ControlLease::update_timeout',155,165),ref('cpp/host/src/simulation_host.cpp','SimulationHost::make_safe_stop_input',315,321),...refs(54)]),
  code('안전 정지에 사용하는 실제 운전 입력', 'cpp/host/src/simulation_host.cpp',315,321,[
    '입력 객체의 기본 스로틀은 0이며, 이 함수는 브레이크와 handbrake를 명시적으로 설정합니다.',
    '안전 정지 상태 선언과 실제 물리 입력 적용은 연결해서 읽어야 합니다.'
  ],[ref('cpp/host/src/simulation_host.cpp','SimulationHost::make_safe_stop_input',315,321),ref('cpp/host/src/simulation_host.cpp','SimulationHost::run_tick',635,648)]),
  code('더 오래 끊긴 연결을 폐기하는 hard timeout',P.lease,170,180,[
    '정상 입력 공백이 hard_timeout_을 넘으면 현재 세션을 폐기 목록에 넣고 소유 키를 지웁니다.',
    '이전 세션의 늦은 명령으로 다시 운전을 시작하지 못하게 합니다.'
  ],[ref(P.lease,'ControlLease::update_hard_timeout',170,180),...refs(54)]),
  page('1000ms 연결 폐기가 추가로 필요한 이유',[
    '172~176행은 아직 정상 입력이 없거나 소유자가 없거나 공백이 한도 이하이면 전환하지 않습니다. 한도를 넘으면 177행이 안전 정지를 유지하고 178행이 이전 세션을 폐기 목록에 넣습니다. 179행은 현재 소유 키를 비웁니다.',
    '호스트는 이 hard timeout 결과를 받아 제어 연결 종료 콜백도 호출합니다. 이제 단순히 같은 세션으로 명령 하나를 다시 보내는 것으로 회복할 수 없습니다. 클라이언트는 새 연결과 수명 절차를 거쳐 정상 제어권을 다시 얻어야 합니다.',
    'soft는 일시 중단 뒤 같은 소유자의 회복을 허용하고 hard는 오래 끊긴 연결을 명확히 차단합니다. 둘을 하나로 합치면 짧은 지연마다 재연결하거나, 반대로 오래된 연결을 계속 믿게 됩니다. 서로 다른 목적 때문에 임계값도 나누어 둡니다.'
  ],[ref(P.lease,'ControlLease::update_hard_timeout',170,180),ref('cpp/host/src/simulation_host.cpp','SimulationHost::run_tick',649,663),...refs(54)]),
  page('교육용 무입력 시간선',[
    '마지막 정상 수신을 서버 5000ms로 두고 이후 정상 입력이 없다고 가정합니다. 표의 now는 서버가 검사를 실행한 시각입니다. 무입력 기간은 그때의 서버 now에서 마지막 정상 수신 시각 5000ms를 빼서 계산합니다.',
    '현재 비교는 한도를 초과할 때 전환합니다. 실제 실행은 검사 함수가 호출되는 틱에 이뤄지므로 정확히 해당 벽시각에 별도 알람 스레드가 강제 실행한다는 뜻은 아닙니다.'
  ],[ref(P.lease,'ControlLease::update_timeout',155,180),...refs(54)],{type:'table',headers:['서버 now','정상 입력 공백','이 조건의 결과'],rows:[['5250ms','250ms','soft 한도 이하'],['5251ms','251ms','SafeStop 전환, 소유 유지'],['6000ms','1000ms','hard 한도 이하'],['6001ms','1001ms','세션 폐기, 재연결 필요']]}),
  page('회복되는 입력과 회복되지 않는 입력',[
    '교육용 마지막 정상 입력은 client 1000ms, server 5000ms입니다. server 5300ms에서 SafeStop 후 client 1410ms의 입력을 server 5410ms에 받으면 두 간격이 410ms로 같아 지연 추정은 0입니다.',
    '같은 소유자이고 다른 조건도 맞으면 수락하여 SafeStop을 해제합니다. 반면 client 1020ms의 옛 명령이 server 5410ms에 오면 410−20=390ms이므로 거부하고 정상 수신 기준을 갱신하지 않습니다.',
    'hard timeout으로 폐기한 연결은 신선한 시각만 넣어도 되살릴 수 없습니다. E-stop도 별도 정책입니다. SafeStop 회복 규칙을 모든 정지 상태의 공통 해제 방법으로 해석하면 실제 안전 상태를 잘못 설명하게 됩니다.'
  ],refs(54,55)),
  code('검증 뒤에만 기록하는 수락 상태',P.lease,145,149,[
    '수락할 때만 최고 순번, 클라이언트 시각, 서버 수신 시각을 갱신합니다.',
    '148행이 일반 SafeStop을 해제하고 Accepted를 반환합니다.'
  ],[ref(P.lease,'ControlLease::accept',145,149),...refs(52,55)]),
  page('확인 문제: 시간값으로 직접 판정',[
    'A. 이전 정상 입력은 client 2000ms/server 9000ms입니다. 다음은 client 2020ms/server 9180ms입니다. 수신 간격, 생성 간격, 지연 추정과 결과를 계산하세요.',
    'B. 이후 정상 입력이 없고 server now가 9300ms와 10100ms입니다. 마지막 정상 수신은 어느 값에 남으며 각각 어떤 전환이 필요한가요?',
    'C. B의 첫 SafeStop 직후 같은 연결에서 fresh 입력이 오면 항상 회복하나요? hard timeout 뒤의 같은 세션, E-stop까지 포함하여 회복 조건의 한계를 설명하세요.'
  ],refs(53,54,55),{type:'quiz'}),
  page('해설: 지연 거부, 안전 정지, 폐기',[
    'A는 서버 간격 180ms, 클라이언트 간격 20ms이므로 지연 추정 160ms입니다. 100ms를 초과하여 거부합니다. 마지막 정상 수신은 9180ms로 바뀌지 않고 9000ms에 남아야 다음 무입력 계산도 올바릅니다.',
    'B의 9300ms는 정상 입력 공백 300ms이므로 SafeStop을 적용하되 소유권을 유지합니다. 10100ms는 공백 1100ms이므로 hard timeout으로 세션을 폐기하고 재연결을 요구합니다. 속도를 순간이동처럼 0으로 만드는 규칙은 아닙니다.',
    'C는 같은 소유자의 순서 있고 지연 한도도 통과하는 정상 입력일 때 회복합니다. 이미 폐기한 세션은 새 연결 절차가 필요하며 E-stop은 별도 정책입니다. 서버 루프 자체가 늦으면 검사 실행도 늦을 수 있으므로 시간 한도를 늘리는 것으로 원인을 숨기면 안 됩니다.'
  ],refs(53,54,55),{type:'answer'}),
]));

lessons.push(lesson({id:'L13',chapter:'C6',title:'한 입력의 왕복과 종합 실습',covers:['V1-56','V1-57','V1-58','V1-59','V1-60','V1-61','V1-62','V1-63','V1-64'],
  problem:'각 함수는 읽었지만 W 입력이 어디서 바이트가 되고 어떤 검사 뒤 화면에 표시되는지 연결하여 설명하기 어렵습니다.',
  why:'부분 지식을 전체 경로에 연결해야 파싱 단위 검사가 실제 연결 수락까지 검증했다고 과장하지 않고 문제 위치를 찾아갈 수 있습니다.',
  goal:'송신·서버 수락·WorldState 작성·Unreal 반영을 현재 함수로 추적하고 종합 문제와 기존 회귀검사를 설명합니다.',
  prerequisite:'L01~L12를 읽고 태그, 필드 부재, 현재 연결, ControlLease 시간 조건을 설명할 수 있어야 합니다.',
  next:'복습할 때는 왕복을 먼저 설명한 뒤 막힌 함수만 찾아 읽습니다. 다음 권은 이 계약 위에서 클라이언트와 시뮬레이션의 실제 동작을 확장해 읽습니다.'},[
  page('추적할 명령과 가정',[
    '교육용으로 현재 정상 연결에서 throttle 0.5, steering 0, gear Drive인 입력을 보낸다고 두겠습니다. 지도와 Hello, Play 초기화가 이미 정상이라는 가정입니다. 이 가정을 적어야 실제로 거부할 수 있는 조건을 설명에서 빠뜨리지 않습니다.',
    '이번 추적의 목표는 입력이 속도 몇 km/h를 반드시 만든다고 계산하는 것이 아닙니다. 서버가 수락한 의도는 지면·차량 상태와 함께 물리 계산에 들어갑니다. 여기서는 어떤 값이 어느 계약과 함수 경계를 통과하는지 확인합니다.',
    '읽는 순서는 클라이언트의 바이트 작성, 서버 해석과 수락, 결과 메시지 작성, 클라이언트 상태 저장입니다. 각 단계에서 성공을 보장하는 범위를 한 문장으로 적으면 조작이 안 될 때 어느 증거가 빠졌는지 파악할 수 있습니다.'
  ],refs(56,57,58)),
  code('현재 클라이언트의 실제 송신',P.client,754,763,[
    'PendingControl과 순번·출처·연결·지도 정보를 SerializeControlEnvelope에 넘깁니다.',
    'Socket->Send의 마지막 true는 바이너리 메시지를 선택합니다.'
  ],refs(56)),
  page('송신 코드의 줄별 해설',[
    '754~759행은 입력과 Envelope 정보를 바이트 배열로 만듭니다. 756행의 OutgoingSequence++는 이번 번호를 전달하고 다음 번호를 준비합니다. 출처와 연결, 지도도 넣으므로 스로틀 조각만 보내는 실습과 다릅니다.',
    '760행은 배열의 시작 주소와 길이를 사용하여 바이너리 전송을 요청합니다. 761~763행은 마지막으로 보낸 입력과 변경 상태를 갱신합니다. 이 저장은 클라이언트의 송신 관리이며 서버가 입력을 수락했다는 응답을 대신하지 않습니다.',
    '서버가 바이트를 해석한 뒤에는 Hello와 지도, 현재 연결, 순번·시간을 검사합니다. L10~L12에서 배운 검사가 여기서 연결됩니다. 따라서 Send 호출을 봤다는 사실과 서버의 정상 수락 기록을 확인했다는 사실을 구분해서 설명해야 합니다.'
  ],refs(56,47,52,55)),
  code('서버가 제어권 결과를 검사하는 자리',P.session,375,385,[
    '수신 정보로 ControlLease::accept를 호출하고 Accepted가 아니면 이유를 남겨 거부합니다.',
    '검증한 입력을 물리에 적용하는 호출은 이 실패 분기 뒤에 있습니다.'
  ],[ref(P.session,'SimulationHost::handle_control_command',375,392)]),
  page('수락과 물리 결과의 연결',[
    '375행은 기존 SafeStop 상태를 보관합니다. 376~379행은 출처와 연결, 순번, 클라이언트 시각, 서버 수신 시각을 제어권 검사에 넘깁니다. 380~385행은 수락하지 않은 명령의 이유를 남기고 물리 입력 적용 전 반환합니다.',
    '정상 경로는 뒤의 apply_vehicle_input으로 이어집니다. 이 입력이 틱 계산에서 소비되면 서버의 차량 상태가 변합니다. 같은 throttle 0.5라도 경사·접지·속도·차량 옵션에 따라 결과가 다르므로 메시지는 의도와 결과를 따로 표현합니다.',
    '디버깅에서는 거부 로그가 없다는 추측보다 수락 상태와 최신 입력 나이, 실제 상태 진행을 확인합니다. 다음 코드는 계산된 차량 상태를 WorldState에 넣습니다. 이 시점부터 서버는 입력 요청 대신 관측할 결과를 보냅니다.'
  ],[ref(P.session,'SimulationHost::handle_control_command',375,392),...refs(57)]),
  code('상태와 Health를 같은 메시지에 작성',P.wire,443,449,[
    'Envelope 안의 WorldState에 차량 상태를 추가하고 Health가 있으면 함께 채웁니다.',
    '뒤에서 NPC·신호·구조물 등을 채운 다음 생성 클래스가 바이트로 직렬화합니다.'
  ],refs(57)),
  page('WorldState 작성 코드의 의미',[
    '443행은 바깥 Envelope를 만들고 444행은 공통 메타데이터를 채웁니다. 445행은 WorldState payload를 선택합니다. 446행은 entities 목록에 플레이어 상태 하나를 추가하고 프로젝트 상태를 메시지 값으로 변환합니다.',
    '447~449행은 Health 포인터가 있을 때 상태를 같이 채웁니다. 차량 모습과 제어 안전 상태를 같은 WorldState에 담으면 클라이언트가 서로 다른 메시지 시점의 정보만 따로 조합하는 문제를 줄일 수 있습니다.',
    '이 발췌 뒤에서는 다른 런타임 엔티티와 신호, 구조물 정보도 작성합니다. 생성 클래스의 SerializeAsString은 완성한 값을 바이트로 표현합니다. 차량 힘을 계산하는 함수와 결과를 포장하는 함수의 책임을 다시 구분해 보세요.'
  ],refs(57)),
  code('검증한 수신 상태를 저장하는 지점',P.client,1099,1107,[
    '이 코드는 파싱과 현재 지도·Play·순번 검사를 통과한 뒤의 성공 경로입니다.',
    '최신 차량 상태와 수신 시각을 저장하고 교통 표시도 같은 수락 흐름에 연결합니다.'
  ],refs(58)),
  page('화면에 쓰기 전의 성공 경계',[
    '1099~1101행은 수락한 상태의 도착 시각과 관측 개수를 갱신합니다. 1102행은 파싱한 차량 상태를 최신 상태로 옮깁니다. 1103~1104행은 그 상태의 수신 시각과 존재 여부를 기록하여 GetLatestState가 유효한 결과를 반환할 수 있게 합니다.',
    '1105~1107행은 교통 상태가 수락됐음을 기록하고 런타임 표시와 신호 갱신을 연결합니다. 이 부분만 따로 떼어 보면 단순 대입이지만, 앞의 거부 분기들이 현재 상태를 보호한다는 맥락이 핵심입니다.',
    'L01에서 읽은 GetLatestState는 이렇게 저장한 값을 요청하는 함수였습니다. 송신한 스로틀과 화면에 표시한 속도 사이에는 입력 수락과 물리 계산, 결과 검증이 있습니다. 이 연결을 말로 설명하면 첫 단원부터의 왕복이 완성됩니다.'
  ],refs(2,58)),
  page('종합 확인 문제: 바이트와 현재 제어',[
    'A. sequence=150을 2번 uint64 필드에 쓰는 태그와 값 바이트를 계산하세요. 5A 05 15 00 00 00 3F의 내부 값도 별도로 계산하고 전체 수락 패킷인지 구분하세요.',
    'B. wheels=21의 두 번째 원소에 wheel_index=3이 있고 gear는 생략됐습니다. 바퀴 위치와 현재 decoder의 기어 결과를 적으세요.',
    'C. 바이트 형식과 큰 순번은 정상이지만 지도 checksum이 다릅니다. 이를 수락해도 되나요? 같은 Play 재연결에서 물리 초기화 여부도 함께 설명하세요.'
  ],refs(59,60,61,62),{type:'quiz'}),
  page('종합 해설: 계산과 수락의 근거',[
    'A에서 태그는 2×8+0=16, 즉 10이고 값 150은 96 01이므로 10 96 01입니다. 중첩 조각의 내부 float는 0.5입니다. 이 조각에는 전체 Envelope와 수명 계약의 필수 정보가 없으므로 수락 가능한 전체 패킷이라고 할 수 없습니다.',
    'B의 wheel_index=3은 뒤오른쪽을 뜻하며 목록 두 번째라는 순서와 구분합니다. gear 부재는 현재 내부 기본 Drive를 유지합니다. C는 지도 계약이 다르므로 높은 순번이어도 수락할 수 없습니다. 큰 번호는 다른 검사를 우회하지 않습니다.',
    '같은 Play의 정상 새 연결은 물리 상태를 보존하고 제어 연결을 교체한 뒤 새 정상 입력을 기다립니다. 새 Play 초기화와 다릅니다. 이제 답을 확인하는 데 그치지 말고 각 결론의 분기가 어느 함수에 있는지 찾아 표시해 보세요.'
  ],refs(60,62),{type:'answer'}),
  code('gear 부재 정책을 확인하는 기존 회귀검사',P.test,954,965,[
    'gear를 넣지 않은 ControlCommand를 만들어 decoder의 결과를 검사합니다.',
    '이 테스트는 기어 해석을 검사하며 실제 소켓·Hello·ControlLease 전체를 실행하지 않습니다.'
  ],refs(63)),
  page('회귀검사의 준비, 실행, 판정',[
    '954~956행은 현재 스키마 버전과 throttle 0.5만 넣은 Envelope를 만듭니다. gear를 생략하는 준비가 핵심입니다. 958~960행은 오류 문자열을 준비하고 직렬화한 메시지를 decoder에 넣어 결과를 받습니다.',
    '962행은 파싱 결과가 있는지 검사합니다. 963~964행은 내부 gear가 Drive인지 확인합니다. 나중에 기본값이나 존재 여부 처리를 바꿨을 때 부재가 중립으로 바뀌는 회귀를 잡으려는 테스트입니다.',
    '이 검사가 통과해도 WebSocket 연결과 현재 Play, 지도 정합, 시간 한도가 정상이라는 증거는 아닙니다. 테스트가 실행한 범위와 검증하지 않은 범위를 함께 설명해야 합니다. 본인의 이해를 확인할 때도 준비와 실행, 기대 결과를 먼저 적어 보세요.'
  ],refs(63)),
  page('혼자 복습하는 순서와 찾을 함수',[
    '코드를 가리고 W 입력의 왕복을 설명합니다. 바이트 작성에서 막히면 SerializeControlEnvelope를, 부재 처리에서 막히면 parse_client_message_envelope를 읽습니다. 설명이 끊긴 지점부터 확인합니다.',
    '오래된 입력은 ControlLease::accept와 timeout 함수를 봅니다. 연결과 Play의 차이는 handle_simulation_reset에서 확인합니다. 화면 반영은 ApplyBinaryMessage의 거부 분기와 LatestState 저장을 함께 봅니다.',
    '150 대신 300, 지연 160ms 대신 100ms, 같은 Play 대신 옛 Play로 바꾸어 다시 풉니다. 결과와 근거의 변화를 설명하세요. 입력을 바꿔 결과와 근거를 예측하는 것이 완료 기준입니다.'
  ],refs(64)),
]));

export default {id:'01',title:'공통 기초와 Protobuf',subtitle:'실제 문제의 이유를 이해하고 코드와 값으로 따라가는 독학 교재',chapters,lessons};
